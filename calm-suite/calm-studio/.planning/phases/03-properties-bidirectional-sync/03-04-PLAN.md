---
phase: 03-properties-bidirectional-sync
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements:
  - PROP-01
  - PROP-02
  - PROP-03
  - PROP-04
  - PROP-05
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - CODE-01
  - CODE-02
  - CODE-03

must_haves:
  truths:
    - "All five Phase 3 success criteria are visually verified by the user"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Phase 3 deliverable: properties panel, code editor, and bidirectional sync working together.

Purpose: Human verification that the bidirectional sync UX works correctly and feels right before marking Phase 3 complete.
Output: User confirmation that all Phase 3 success criteria are met.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-properties-bidirectional-sync/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and prepare verification environment</name>
  <files></files>
  <action>
Start the development server:
```bash
cd apps/studio && pnpm dev
```
Confirm it starts without errors. Note the URL (typically http://localhost:5173).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio build 2>&1 | tail -3</automated>
  </verify>
  <done>Dev server is running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 3 deliverable</name>
  <action>
Present the running application to the user for verification of all Phase 3 success criteria. The dev server from Task 1 should be running at http://localhost:5173.
  </action>
  <verify>User confirms all criteria pass or describes issues.</verify>
  <done>User types "approved" confirming all Phase 3 success criteria are met.</done>
  <what-built>Complete Phase 3: Properties panel (right sidebar), CALM JSON code editor (bottom panel), and bidirectional sync engine connecting canvas, code, and properties.</what-built>
  <how-to-verify>
    Open http://localhost:5173 and verify each criterion:

    **1. Properties Panel (right sidebar)**
    1.1. With nothing selected: properties panel shows collapsed/empty state
    1.2. Drag a "service" node onto canvas. Click it. Properties panel shows: unique-id (read-only), name field, description field, node-type dropdown (showing "service")
    1.3. Change name to "Payment Service" — canvas node label updates immediately
    1.4. Change node-type dropdown to "database" — canvas node shape changes to database shape
    1.5. Click "+Add Interface" — new row appears with type dropdown and value input
    1.6. Set interface type to "url", value to "https://api.example.com" — verify it persists
    1.7. Click "+Add Property" in Custom Properties section — add key "team", value "payments"
    1.8. Verify "Controls" section shows disabled with "Coming in Phase 6" message

    **2. Edge Properties**
    2.1. Add a second node. Draw an edge between the two nodes
    2.2. Click the edge. Properties panel shows: relationship-type dropdown, protocol field, description
    2.3. Change relationship-type to "interacts" — edge style updates on canvas

    **3. Code Editor (bottom panel)**
    3.1. Code panel is visible below the canvas by default
    3.2. CALM JSON tab is active; calmscript tab is grayed out with tooltip "Coming in Phase 5"
    3.3. JSON has syntax highlighting (colored keys/values) and line numbers
    3.4. The JSON reflects the two nodes and edge you created

    **4. Bidirectional Sync**
    4.1. Edit a node name in the code editor JSON — after a brief pause, canvas node label updates
    4.2. Type invalid JSON (delete a closing brace) — red squiggles appear, canvas keeps showing last valid state
    4.3. Fix the JSON — canvas updates again
    4.4. Move a node on the canvas — code editor JSON updates (though positions aren't in CALM JSON, the node data is current)
    4.5. Click a node on canvas — code editor scrolls to highlight that node's JSON fragment

    **5. Undo/Redo**
    5.1. Make several changes across surfaces (rename in properties, add node via palette, edit JSON)
    5.2. Press Cmd+Z multiple times — changes undo in reverse order regardless of which surface was used
    5.3. Press Cmd+Shift+Z — changes redo correctly

    **6. Layout**
    6.1. Drag the divider between palette and canvas — both resize smoothly
    6.2. Drag the divider between canvas and code editor — both resize
    6.3. Drag the divider between canvas and properties panel — both resize
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all Phase 3 success criteria.
</verification>

<success_criteria>
- User confirms all 6 verification groups pass
- No infinite loops observed during rapid editing
- UI feels responsive (no visible lag on sync)
</success_criteria>

<output>
After completion, create `.planning/phases/03-properties-bidirectional-sync/03-04-SUMMARY.md`
</output>
