---
phase: 06-calm-validation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/calm-core/package.json
  - packages/calm-core/src/schemas/calm.json
  - packages/calm-core/src/schemas/core.json
  - packages/calm-core/src/schemas/interface.json
  - packages/calm-core/src/schemas/flow.json
  - packages/calm-core/src/schemas/control.json
  - packages/calm-core/src/schemas/control-requirement.json
  - packages/calm-core/src/schemas/evidence.json
  - packages/calm-core/src/schemas/units.json
  - packages/calm-core/src/validation.ts
  - packages/calm-core/src/index.ts
  - packages/calm-core/src/validation.test.ts
  - packages/calm-core/vite.config.ts
autonomous: true
requirements:
  - VALD-01
  - VALD-03

must_haves:
  truths:
    - "validateCalmArchitecture() returns error issues for missing required CALM fields"
    - "validateCalmArchitecture() returns warning issues for orphan nodes and self-loops"
    - "validateCalmArchitecture() returns info issues for nodes missing description"
    - "Ajv schema validation catches structural violations (wrong types, missing arrays)"
    - "Semantic rules catch dangling refs, duplicates, orphans, self-loops"
  artifacts:
    - path: "packages/calm-core/src/validation.ts"
      provides: "Shared Ajv-based validation engine"
      exports: ["validateCalmArchitecture", "ValidationIssue"]
    - path: "packages/calm-core/src/schemas/calm.json"
      provides: "Bundled CALM 2025-03 top-level schema"
    - path: "packages/calm-core/src/validation.test.ts"
      provides: "Unit tests for validation engine"
      min_lines: 50
  key_links:
    - from: "packages/calm-core/src/validation.ts"
      to: "packages/calm-core/src/schemas/calm.json"
      via: "static JSON import for Ajv compile"
      pattern: "import.*schemas/calm"
    - from: "packages/calm-core/src/index.ts"
      to: "packages/calm-core/src/validation.ts"
      via: "re-export"
      pattern: "export.*validation"
---

<objective>
Install Ajv, bundle CALM 2025-03 JSON schemas, create the shared validation engine in calm-core, and verify it with unit tests.

Purpose: This is the foundation — both the studio reactive store and the MCP server will import `validateCalmArchitecture()` from calm-core. Getting the Ajv setup and schema bundling right here prevents issues downstream.
Output: `packages/calm-core/src/validation.ts` exporting `validateCalmArchitecture()` and `ValidationIssue` type, with passing tests.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-calm-validation/06-CONTEXT.md
@.planning/phases/06-calm-validation/06-RESEARCH.md

<interfaces>
<!-- Key types from calm-core that the validation engine operates on -->

From packages/calm-core/src/types.ts:
```typescript
export interface CalmNode {
  'unique-id': string;
  'node-type': CalmNodeType | string;
  name: string;
  description?: string;
  interfaces?: CalmInterface[];
  customMetadata?: Record<string, string>;
}

export interface CalmRelationship {
  'unique-id': string;
  'relationship-type': CalmRelationshipType;
  source: string;
  destination: string;
  protocol?: string;
  description?: string;
}

export interface CalmArchitecture {
  nodes: CalmNode[];
  relationships: CalmRelationship[];
}
```

From packages/mcp-server/src/validation.ts (existing rules to preserve):
```typescript
export interface ValidationIssue {
  severity: 'error' | 'warning';
  message: string;
  nodeId?: string;
  relationshipId?: string;
}

// Rules to migrate:
// Errors: missing unique-id/name, missing source/destination, dangling refs, duplicate IDs
// Warnings: orphan nodes, self-loops
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Ajv, bundle CALM schemas, create validation engine with tests</name>
  <files>
    packages/calm-core/package.json,
    packages/calm-core/vite.config.ts,
    packages/calm-core/src/schemas/calm.json,
    packages/calm-core/src/schemas/core.json,
    packages/calm-core/src/schemas/interface.json,
    packages/calm-core/src/schemas/flow.json,
    packages/calm-core/src/schemas/control.json,
    packages/calm-core/src/schemas/control-requirement.json,
    packages/calm-core/src/schemas/evidence.json,
    packages/calm-core/src/schemas/units.json,
    packages/calm-core/src/validation.ts,
    packages/calm-core/src/validation.test.ts,
    packages/calm-core/src/index.ts
  </files>
  <behavior>
    - Test: valid architecture with 2 nodes + 1 relationship returns empty issues array
    - Test: node missing unique-id returns error with nodeId undefined
    - Test: node missing name returns error
    - Test: relationship with dangling source ref returns error with relationshipId
    - Test: duplicate node unique-ids returns error
    - Test: orphan node (no relationships) returns warning with nodeId
    - Test: self-loop relationship returns warning with relationshipId
    - Test: node missing description returns info issue
    - Test: empty architecture ({nodes:[], relationships:[]}) returns no errors
    - Test: Ajv rejects architecture with wrong types (nodes is string, not array)
  </behavior>
  <action>
    1. Install ajv and ajv-formats in calm-core: `pnpm --filter @calmstudio/calm-core add ajv ajv-formats`
    2. Install vitest as devDep: `pnpm --filter @calmstudio/calm-core add -D vitest`
    3. Create `packages/calm-core/vite.config.ts` with vitest config (minimal — no SvelteKit needed for this package)
    4. Update `packages/calm-core/package.json` test script to `vitest run`
    5. Download all 8 CALM 2025-03 meta schema JSON files from `https://raw.githubusercontent.com/finos/architecture-as-code/main/calm/draft/2025-03/meta/` and save to `packages/calm-core/src/schemas/`. Files: calm.json, core.json, interface.json, flow.json, control.json, control-requirement.json, evidence.json, units.json.
    6. Create `packages/calm-core/src/validation.ts`:
       - Import Ajv (default import), addFormats from ajv-formats
       - Import all 8 schema JSON files as static imports
       - Create single Ajv instance: `new Ajv({ allErrors: true, strict: false })`
       - Call `addFormats(ajv)`
       - Register all sub-schemas via `ajv.addSchema()` (core, interface, flow, control, control-requirement, evidence, units)
       - Compile top-level validator: `ajv.compile(calmSchema)`
       - Export `ValidationIssue` interface with severity `'error' | 'warning' | 'info'`, message, optional nodeId, optional relationshipId, optional path (string)
       - Export `validateCalmArchitecture(arch: CalmArchitecture): ValidationIssue[]` that:
         a. Runs Ajv schema validation — maps errors to ValidationIssue with severity 'error', extracting nodeId/relationshipId from instancePath where possible
         b. Runs semantic validation (ported from mcp-server/src/validation.ts): missing fields, dangling refs, duplicate IDs, orphan nodes, self-loops
         c. Adds info-level rules: node with no description -> info
       - Helper functions: `extractNodeId(instancePath, arch)` and `extractRelationshipId(instancePath, arch)` to map Ajv instancePath like `/nodes/0/name` to the actual node unique-id
    7. Update `packages/calm-core/src/index.ts` to add: `export * from './validation.js';`
    8. Create `packages/calm-core/src/validation.test.ts` with the behavior tests listed above.

    IMPORTANT: If CALM schema $ref URIs do not resolve correctly with bundled copies, register schemas using their $id field: `ajv.addSchema(schema, schema.$id ?? schema['$id'])`. If schemas use 2020-12 vocabulary identifiers that Ajv strict mode rejects, `strict: false` handles this — do NOT try to register custom keywords.

    NOTE: The existing mcp-server validation.ts will be updated to import from calm-core in Plan 02. Do NOT modify mcp-server in this plan.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/calm-core test</automated>
  </verify>
  <done>
    - validateCalmArchitecture() exported from @calmstudio/calm-core
    - ValidationIssue type includes 'info' severity level
    - All 10+ unit tests pass covering error, warning, and info cases
    - Ajv compiles CALM 2025-03 schemas without errors
    - Semantic rules (dangling refs, duplicates, orphans, self-loops) produce correct issues
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/calm-core test
```
All tests green. `validateCalmArchitecture` importable from `@calmstudio/calm-core`.
</verification>

<success_criteria>
- calm-core exports `validateCalmArchitecture()` and `ValidationIssue`
- Ajv compiles all 8 CALM 2025-03 schemas without error
- Unit tests cover valid, error, warning, and info scenarios
- `pnpm --filter @calmstudio/calm-core test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-calm-validation/06-00-SUMMARY.md`
</output>
