---
phase: 02-calm-canvas-core
plan: 04
type: execute
wave: 3
depends_on: ["02-00", "02-02", "02-03"]
files_modified:
  - apps/studio/src/lib/canvas/CalmCanvas.svelte
  - apps/studio/src/lib/palette/NodePalette.svelte
  - apps/studio/src/lib/palette/DnDProvider.svelte
  - apps/studio/src/lib/canvas/containment.ts
  - apps/studio/src/routes/+page.svelte
  - apps/studio/src/tests/containment.test.ts
autonomous: true
requirements:
  - CANV-01
  - CANV-02
  - CANV-03
  - CANV-04
  - CALM-05

must_haves:
  truths:
    - "User can drag any of the 9 CALM node types from the palette onto the canvas"
    - "User can click a palette item to place a node at canvas center"
    - "User can draw edges between nodes by dragging from handle to handle"
    - "New edges default to 'connects' type"
    - "User can select, multi-select, move, resize, and delete nodes"
    - "User can zoom and pan the canvas"
    - "Dragging a node into a container or drawing a deployed-in/composed-of edge creates containment"
  artifacts:
    - path: "apps/studio/src/lib/canvas/CalmCanvas.svelte"
      provides: "SvelteFlow wrapper with nodeTypes, edgeTypes, event handlers"
      min_lines: 50
    - path: "apps/studio/src/lib/palette/NodePalette.svelte"
      provides: "Left sidebar with 9 CALM types + Custom entry, search, drag-and-drop + click-to-place"
      min_lines: 40
    - path: "apps/studio/src/lib/canvas/containment.ts"
      provides: "makeContainment() and removeContainment() functions"
      exports: ["makeContainment", "removeContainment"]
    - path: "apps/studio/src/routes/+page.svelte"
      provides: "Main layout: sidebar palette + canvas area"
      min_lines: 20
  key_links:
    - from: "apps/studio/src/lib/palette/NodePalette.svelte"
      to: "apps/studio/src/lib/canvas/CalmCanvas.svelte"
      via: "HTML5 DnD dataTransfer with 'application/calm-node-type'"
      pattern: "application/calm-node-type"
    - from: "apps/studio/src/lib/canvas/CalmCanvas.svelte"
      to: "apps/studio/src/lib/canvas/containment.ts"
      via: "onconnect handler calling makeContainment for deployed-in/composed-of edges"
      pattern: "makeContainment"
---

<objective>
Wire the canvas, palette, and containment logic into a working diagramming surface where users can create nodes (drag or click), draw edges, select/move/delete, zoom/pan, and establish containment relationships.

Purpose: This is the core interaction loop — the canvas becomes usable as a diagramming tool.
Output: Working drag-and-drop palette, interactive canvas with all node/edge types, containment via sub-flows.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calm-canvas-core/02-RESEARCH.md
@.planning/phases/02-calm-canvas-core/02-CONTEXT.md
@.planning/phases/02-calm-canvas-core/02-01-SUMMARY.md
@.planning/phases/02-calm-canvas-core/02-02-SUMMARY.md
@.planning/phases/02-calm-canvas-core/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 02: apps/studio/src/lib/canvas/nodeTypes.ts -->
```typescript
export const nodeTypes = { actor, system, service, database, network, webclient, ecosystem, ldap, 'data-asset', generic, container } as const;
export function resolveNodeType(calmType: string): string;
```

<!-- From Plan 03: apps/studio/src/lib/canvas/edgeTypes.ts -->
```typescript
export const edgeTypes = { connects, interacts, 'deployed-in', 'composed-of', options } as const;
export const DEFAULT_EDGE_TYPE = 'connects';
```

<!-- From @xyflow/svelte -->
```typescript
import { SvelteFlow, useSvelteFlow, type Node, type Edge } from '@xyflow/svelte';
// useSvelteFlow() provides: screenToFlowPosition(), getNodes(), fitView()
// SvelteFlow props: bind:nodes, bind:edges, nodeTypes, edgeTypes, onconnect, onnodedragstop, deleteKey, selectionKey, fitView
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NodePalette sidebar with DnD and click-to-place</name>
  <files>
    apps/studio/src/lib/palette/NodePalette.svelte,
    apps/studio/src/lib/palette/DnDProvider.svelte
  </files>
  <action>
    1. Create `DnDProvider.svelte` — a Svelte context wrapper that stores the currently dragged node type. Uses `setContext`/`getContext` with a string key. Exports a `useDnD()` function that returns `{ dragType, setDragType }` using Svelte 5 `$state`.

    2. Create `NodePalette.svelte` — left sidebar panel (per user decision: always visible, search at top):

       **Layout:**
       - Fixed-width left sidebar (~240px)
       - Search input at top (filters palette items by name)
       - List of palette entries below search
       - 9 core CALM types: actor, system, service, database, network, webclient, ecosystem, ldap, data-asset
       - "Custom..." entry at bottom that prompts for a type string (use a simple input/popover)

       **Each palette item:**
       - Shows mini shape icon (small SVG matching the node's shape) + type name label (per user decision)
       - `draggable="true"` with `ondragstart` setting `dataTransfer.setData('application/calm-node-type', type)` and `effectAllowed = 'copy'`
       - `onclick` handler for click-to-place: dispatches a `placenode` CustomEvent on the component with `{ detail: { type } }`. The parent (+page.svelte) listens to this event and creates a node at canvas center via `screenToFlowPosition()`.

       **Search:**
       - Simple substring filter on type names (no fuse.js needed here — fuse.js is for canvas node search in Plan 05)
       - Empty search shows all entries

       **Custom node entry:**
       - Clicking "Custom..." shows an inline text input
       - User types a custom type string and presses Enter
       - Creates a node with `type: 'generic'` and `data.calmType` set to the custom string

    Style with Tailwind. Include Apache 2.0 SPDX headers.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio build</automated>
  </verify>
  <done>NodePalette renders 9 CALM types + Custom entry with mini icons, search filtering, drag-and-drop support, and click-to-place via CustomEvent dispatch</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CalmCanvas wrapper with DnD drop handling, edge creation, and containment</name>
  <files>
    apps/studio/src/lib/canvas/CalmCanvas.svelte,
    apps/studio/src/lib/canvas/containment.ts,
    apps/studio/src/routes/+page.svelte,
    apps/studio/src/tests/containment.test.ts
  </files>
  <behavior>
    - makeContainment sets parentId and extent:'parent' on child node
    - makeContainment converts parent to container type if not already
    - removeContainment clears parentId and extent
    - isContainmentType returns true for 'deployed-in' and 'composed-of'
    - isContainmentType returns false for 'connects', 'interacts', 'options'
  </behavior>
  <action>
    1. **First: Implement containment.test.ts** — replace the stub test file (created in Plan 00) with real test assertions:
       - Import `makeContainment`, `removeContainment`, `isContainmentType` from `$lib/canvas/containment`
       - Write tests for each behavior listed above using concrete Node[] fixtures
       - Run tests — they should FAIL (RED phase)

    2. Create `containment.ts` with pure functions:
       ```typescript
       // makeContainment: sets parentId + extent:'parent' on child, ensures parent is 'container' type
       export function makeContainment(parentId: string, childId: string, nodes: Node[]): Node[];
       // removeContainment: clears parentId + extent on child
       export function removeContainment(childId: string, nodes: Node[]): Node[];
       // isContainmentType: returns true for 'deployed-in' and 'composed-of'
       export function isContainmentType(edgeType: string): boolean;
       ```
       - Run tests — they should PASS (GREEN phase)

    3. Create `CalmCanvas.svelte` — the main canvas wrapper component:

       **Props:** `bind:nodes`, `bind:edges` (passed from page), `onplacenode` callback prop for click-to-place

       **SvelteFlow configuration:**
       - Pass `nodeTypes` (from Plan 02) and `edgeTypes` (from Plan 03)
       - `deleteKey="Delete"` — Delete key removes selection (CANV-03)
       - `selectionKey="Shift"` — Shift for box select
       - `multiSelectionKey="Meta"` — Cmd/Ctrl for additive select
       - `fitView` — auto-fit on initial render
       - `zoomOnScroll={true}`, `panOnDrag={true}`, `panOnScroll={true}` — standard zoom/pan (CANV-04)

       **Drop handler (ondragover + ondrop on wrapper div):**
       - `ondragover`: `e.preventDefault(); e.dataTransfer.dropEffect = 'copy'`
       - `ondrop`: Read `e.dataTransfer.getData('application/calm-node-type')`, convert screen coords via `screenToFlowPosition()`, create new node with `nanoid()` id, resolved type via `resolveNodeType()`, and `data: { label: 'New {type}', calmId: nanoid(), calmType: type }`
       - MUST use `$state.raw` for nodes/edges — NEVER `$state` (RESEARCH Pitfall 1)

       **Edge creation (onconnect):**
       - New edges get `type: DEFAULT_EDGE_TYPE` ('connects')
       - If the edge type is containment (`isContainmentType`), also call `makeContainment()` to set `parentId` on target node (RESEARCH Pitfall 4 — edge alone doesn't create visual containment)

       **Node drop into container:**
       - On `onnodedragstop`, check if the dropped node overlaps a container node's bounds. If so, call `makeContainment()` to reparent (per user decision: both drag-into AND edge-draw create containment)

       **Render EdgeMarkers.svelte once** inside the canvas wrapper (not per-edge)

       **Click-to-place handler:**
       - Accept an `onplacenode` callback prop: `let { onplacenode }: { onplacenode?: (type: string) => void } = $props();`
       - When called, create node at viewport center using `screenToFlowPosition({ x: window.innerWidth / 2, y: window.innerHeight / 2 })`

    4. Update `+page.svelte` to compose the full layout:
       - Import `DnDProvider`, `NodePalette`, `CalmCanvas`
       - Layout: flexbox with `NodePalette` on left (fixed width) and `CalmCanvas` filling remaining space (flex-1, full height)
       - `DnDProvider` wraps both palette and canvas
       - Manage `nodes` and `edges` state at page level with `$state.raw([])`
       - Pass `bind:nodes` and `bind:edges` to CalmCanvas
       - Listen to NodePalette's `placenode` CustomEvent and forward the type to CalmCanvas's `onplacenode` prop

    All files MUST include Apache 2.0 SPDX headers.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio test --run src/tests/containment.test.ts && pnpm --filter @calmstudio/studio build</automated>
  </verify>
  <done>Containment unit tests pass. Canvas renders with palette sidebar. Dragging from palette creates typed nodes. Drawing handle-to-handle creates 'connects' edges. Containment edges set parentId. Nodes are selectable, movable, resizable, deletable. Zoom/pan works. Click-to-place creates node at viewport center via onplacenode callback prop.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @calmstudio/studio test --run src/tests/containment.test.ts` passes
- `pnpm --filter @calmstudio/studio build` passes
- `+page.svelte` renders sidebar + canvas layout
- DnD from palette to canvas creates a node at drop position
- Click palette item dispatches placenode event, node appears at canvas center
- Handle-to-handle drag creates edge (defaults to 'connects')
- Delete key removes selected nodes/edges
- Zoom (scroll) and pan (drag) work
- deployed-in/composed-of edges trigger containment (parentId set)
</verification>

<success_criteria>
Users can drag nodes from palette, draw edges between them, select/move/delete, zoom/pan, and containment relationships visually nest children inside parent nodes. Containment logic is covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-calm-canvas-core/02-04-SUMMARY.md`
</output>
