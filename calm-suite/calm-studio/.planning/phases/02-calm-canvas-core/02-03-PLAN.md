---
phase: 02-calm-canvas-core
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/studio/src/lib/canvas/edges/ConnectsEdge.svelte
  - apps/studio/src/lib/canvas/edges/InteractsEdge.svelte
  - apps/studio/src/lib/canvas/edges/DeployedInEdge.svelte
  - apps/studio/src/lib/canvas/edges/ComposedOfEdge.svelte
  - apps/studio/src/lib/canvas/edges/OptionsEdge.svelte
  - apps/studio/src/lib/canvas/edges/EdgeMarkers.svelte
  - apps/studio/src/lib/canvas/edgeTypes.ts
autonomous: true
requirements:
  - CALM-03
  - CALM-06

must_haves:
  truths:
    - "Each of the 5 CALM relationship types renders with a distinct line style and marker"
    - "Protocol labels display as inline text on connects edges"
    - "SVG markers (arrows, diamonds) rotate correctly with edge direction"
  artifacts:
    - path: "apps/studio/src/lib/canvas/edges/ConnectsEdge.svelte"
      provides: "Solid line + filled arrow + protocol label"
      min_lines: 15
    - path: "apps/studio/src/lib/canvas/edges/EdgeMarkers.svelte"
      provides: "Shared SVG marker definitions (arrow-filled, diamond-open, diamond-filled, arrow-open)"
      min_lines: 20
    - path: "apps/studio/src/lib/canvas/edgeTypes.ts"
      provides: "edgeTypes map registering all 5 edge components"
      exports: ["edgeTypes"]
  key_links:
    - from: "apps/studio/src/lib/canvas/edges/*.svelte"
      to: "apps/studio/src/lib/canvas/edges/EdgeMarkers.svelte"
      via: "url(#marker-id) references in markerEnd prop"
      pattern: "markerEnd.*url\\(#marker-"
---

<objective>
Create all 5 custom Svelte edge components with distinct line styles and arrow/diamond markers, shared SVG marker definitions, and the edgeTypes registration map.

Purpose: Edge visual differentiation is how architects distinguish relationship types at a glance. Line style + marker shape must match the user's locked design decisions exactly.
Output: 5 edge .svelte files + EdgeMarkers.svelte + edgeTypes.ts.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calm-canvas-core/02-RESEARCH.md
@.planning/phases/02-calm-canvas-core/02-CONTEXT.md
@.planning/phases/02-calm-canvas-core/02-01-SUMMARY.md

<interfaces>
<!-- From @xyflow/svelte (library) -->
```typescript
import { BaseEdge, EdgeLabel, getSmoothStepPath, type EdgeProps } from '@xyflow/svelte';
// EdgeProps provides: { id, sourceX, sourceY, targetX, targetY, label, data, ... }
// BaseEdge renders the SVG path
// EdgeLabel positions text at midpoint
```

<!-- From packages/calm-core/src/types.ts (created in Plan 01) -->
```typescript
export type CalmRelationshipType =
  | 'connects' | 'interacts' | 'deployed-in' | 'composed-of' | 'options';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EdgeMarkers SVG definitions and all 5 custom edge components</name>
  <files>
    apps/studio/src/lib/canvas/edges/EdgeMarkers.svelte,
    apps/studio/src/lib/canvas/edges/ConnectsEdge.svelte,
    apps/studio/src/lib/canvas/edges/InteractsEdge.svelte,
    apps/studio/src/lib/canvas/edges/DeployedInEdge.svelte,
    apps/studio/src/lib/canvas/edges/ComposedOfEdge.svelte,
    apps/studio/src/lib/canvas/edges/OptionsEdge.svelte
  </files>
  <action>
    1. Create `EdgeMarkers.svelte` — rendered ONCE in the canvas DOM. Contains an invisible `<svg>` with `<defs>`:
       - `#marker-arrow-filled`: filled arrowhead (for connects, interacts)
       - `#marker-diamond-open`: open diamond (for deployed-in)
       - `#marker-diamond-filled`: filled diamond (for composed-of)
       - `#marker-arrow-open`: open arrowhead (for options)
       - ALL markers MUST have `orient="auto-start-reverse"` (see RESEARCH Pitfall 5 — without this, markers don't rotate with edge direction)
       - Use `currentColor` for fill/stroke so markers inherit from edge color (works with dark mode)

    2. Create 5 edge components. Each uses `BaseEdge` from `@xyflow/svelte` with `getSmoothStepPath` for path calculation. Line styles per user's locked decision:

       **ConnectsEdge.svelte** — solid line + filled arrow:
       - No stroke-dasharray (solid)
       - `markerEnd="url(#marker-arrow-filled)"`
       - If `data.protocol` or `label` exists, render `<EdgeLabel>` centered on path with protocol text (e.g., "HTTPS", "JDBC", "gRPC")
       - Label has `nodrag nopan` classes to prevent interfering with edge interaction

       **InteractsEdge.svelte** — dashed line + filled arrow:
       - `style="stroke-dasharray: 6 4;"` on BaseEdge
       - `markerEnd="url(#marker-arrow-filled)"`
       - No protocol label (interacts edges don't have protocols)

       **DeployedInEdge.svelte** — solid line + open diamond:
       - No stroke-dasharray (solid)
       - `markerEnd="url(#marker-diamond-open)"`

       **ComposedOfEdge.svelte** — dashed line + filled diamond:
       - `style="stroke-dasharray: 6 4;"`
       - `markerEnd="url(#marker-diamond-filled)"`

       **OptionsEdge.svelte** — dotted line + open arrow:
       - `style="stroke-dasharray: 2 4;"`
       - `markerEnd="url(#marker-arrow-open)"`

    Each edge component receives `EdgeProps` via `$props()` and computes the path with `getSmoothStepPath({ sourceX, sourceY, targetX, targetY })`.

    All files MUST include Apache 2.0 SPDX headers.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio build</automated>
  </verify>
  <done>5 edge components exist with correct line styles (solid/dashed/dotted) and marker references (arrow/diamond). EdgeMarkers.svelte defines all 4 SVG markers with orient="auto-start-reverse". ConnectsEdge shows protocol labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create edgeTypes registration map</name>
  <files>
    apps/studio/src/lib/canvas/edgeTypes.ts
  </files>
  <action>
    Create `apps/studio/src/lib/canvas/edgeTypes.ts` that:

    1. Imports all 5 edge components from `./edges/`
    2. Exports an `edgeTypes` object mapping CALM relationship type strings to Svelte components:
       ```typescript
       export const edgeTypes = {
         connects: ConnectsEdge,
         interacts: InteractsEdge,
         'deployed-in': DeployedInEdge,
         'composed-of': ComposedOfEdge,
         options: OptionsEdge,
       } as const;
       ```
    3. Export `DEFAULT_EDGE_TYPE = 'connects' as const` — per user decision, new edges default to 'connects' type

    Include Apache 2.0 SPDX header.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio build</automated>
  </verify>
  <done>edgeTypes map exports all 5 edge type registrations, DEFAULT_EDGE_TYPE is 'connects'</done>
</task>

</tasks>

<verification>
- 5 edge `.svelte` files + EdgeMarkers.svelte exist in `apps/studio/src/lib/canvas/edges/`
- `edgeTypes.ts` exports `edgeTypes` with 5 entries and `DEFAULT_EDGE_TYPE`
- `pnpm --filter @calmstudio/studio build` passes
- ConnectsEdge: solid + filled arrow + protocol label
- InteractsEdge: dashed + filled arrow
- DeployedInEdge: solid + open diamond
- ComposedOfEdge: dashed + filled diamond
- OptionsEdge: dotted + open arrow
</verification>

<success_criteria>
5 custom edge components with visually distinct line styles and markers are registered in edgeTypes map and the app builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-calm-canvas-core/02-03-SUMMARY.md`
</output>
