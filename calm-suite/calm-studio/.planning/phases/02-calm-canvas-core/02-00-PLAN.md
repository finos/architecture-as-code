---
phase: 02-calm-canvas-core
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/studio/vite.config.ts
  - apps/studio/playwright.config.ts
  - apps/studio/package.json
  - apps/studio/src/tests/containment.test.ts
  - apps/studio/src/tests/history.test.ts
  - apps/studio/src/tests/clipboard.test.ts
  - apps/studio/src/tests/search.test.ts
autonomous: true
requirements:
  - CANV-01
  - CANV-05
  - CANV-07
  - CANV-08
  - CANV-09
  - CALM-05

must_haves:
  truths:
    - "pnpm --filter @calmstudio/studio test --run executes and passes with zero failures"
    - "Stub test files exist for containment, history, clipboard, search, and dark mode"
    - "Vitest is configured in vite.config.ts with jsdom environment"
  artifacts:
    - path: "apps/studio/vite.config.ts"
      provides: "Vitest inline config with jsdom environment"
      contains: "test"
    - path: "apps/studio/playwright.config.ts"
      provides: "Playwright E2E config for future E2E tests"
      min_lines: 10
    - path: "apps/studio/src/tests/containment.test.ts"
      provides: "Stub unit test for containment logic"
      min_lines: 5
    - path: "apps/studio/src/tests/history.test.ts"
      provides: "Stub unit test for undo/redo history"
      min_lines: 5
    - path: "apps/studio/src/tests/clipboard.test.ts"
      provides: "Stub unit test for copy/paste"
      min_lines: 5
    - path: "apps/studio/src/tests/search.test.ts"
      provides: "Stub unit test for node search"
      min_lines: 5
  key_links:
    - from: "apps/studio/vite.config.ts"
      to: "vitest"
      via: "inline test config"
      pattern: "test.*include"
---

<objective>
Set up test infrastructure (Vitest + Playwright) and create stub test files for all unit-testable modules so that subsequent plans can add behavioral tests and use test commands in their verify blocks.

Purpose: Wave 0 test scaffolding is required before any plan can use unit/component test commands for verification. Without this, all verify blocks fall back to build-only checks.
Output: Working test runner, stub test files, Playwright config.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calm-canvas-core/02-RESEARCH.md
@.planning/phases/02-calm-canvas-core/02-CONTEXT.md
@.planning/phases/02-calm-canvas-core/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and configure Vitest + Playwright</name>
  <files>
    apps/studio/package.json,
    apps/studio/vite.config.ts,
    apps/studio/playwright.config.ts
  </files>
  <action>
    1. Add test devDependencies to `apps/studio/package.json`:
       - `vitest` (^3.x), `@testing-library/svelte` (^5.x), `jsdom` (^25.x)
       - `@playwright/test` (^1.x)
       - Add script: `"test": "vitest run"`, `"test:e2e": "playwright test"`

    2. Update `apps/studio/vite.config.ts` to include inline vitest config:
       ```typescript
       test: {
         include: ['src/**/*.test.ts'],
         environment: 'jsdom',
         globals: true,
       }
       ```
       NOTE: This file may already exist from Plan 01. If so, MERGE the test config into the existing file — do not overwrite the sveltekit plugin or ssr.noExternal config.

    3. Create `apps/studio/playwright.config.ts`:
       - Set `testDir: './src/tests/e2e'`
       - Set `webServer: { command: 'pnpm dev', port: 5173 }`
       - Use `chromium` project only (keep it simple)
       - Set `retries: 0` for local dev

    4. Run `pnpm install` from workspace root.

    All files MUST include Apache 2.0 SPDX headers.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm install && pnpm --filter @calmstudio/studio test --run 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest runs successfully (0 tests, 0 failures). Playwright config exists. Test dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for all unit-testable modules</name>
  <files>
    apps/studio/src/tests/containment.test.ts,
    apps/studio/src/tests/history.test.ts,
    apps/studio/src/tests/clipboard.test.ts,
    apps/studio/src/tests/search.test.ts
  </files>
  <action>
    Create stub test files that import from their future module paths and contain `describe` + `test.todo` blocks for each expected behavior. These stubs will PASS (todo tests are skipped, not failures) and serve as scaffolding that Plans 04 and 05 will fill in with real assertions.

    1. Create `apps/studio/src/tests/containment.test.ts`:
       ```typescript
       import { describe, test } from 'vitest';
       // import { makeContainment, removeContainment, isContainmentType } from '$lib/canvas/containment';

       describe('containment', () => {
         test.todo('makeContainment sets parentId and extent on child node');
         test.todo('makeContainment converts parent to container type if not already');
         test.todo('removeContainment clears parentId and extent');
         test.todo('isContainmentType returns true for deployed-in and composed-of');
         test.todo('isContainmentType returns false for connects, interacts, options');
       });
       ```

    2. Create `apps/studio/src/tests/history.test.ts`:
       ```typescript
       import { describe, test } from 'vitest';

       describe('history - undo/redo', () => {
         test.todo('pushSnapshot adds snapshot to history stack');
         test.todo('undo returns previous snapshot');
         test.todo('undo at start of history returns null');
         test.todo('redo returns next snapshot');
         test.todo('redo at end of history returns null');
         test.todo('pushSnapshot after undo drops future history');
       });
       ```

    3. Create `apps/studio/src/tests/clipboard.test.ts`:
       ```typescript
       import { describe, test } from 'vitest';

       describe('clipboard - copy/paste', () => {
         test.todo('copy stores selected nodes');
         test.todo('paste returns cloned nodes with new IDs');
         test.todo('paste offsets position by +20,+20');
         test.todo('paste generates new calmId for each node');
         test.todo('hasClipboard is false when nothing copied');
       });
       ```

    4. Create `apps/studio/src/tests/search.test.ts`:
       ```typescript
       import { describe, test } from 'vitest';

       describe('node search', () => {
         test.todo('createNodeSearcher creates Fuse instance');
         test.todo('searchNodes returns matching node IDs by name');
         test.todo('searchNodes returns matching node IDs by type');
         test.todo('empty query returns no matches');
       });
       ```

    All files MUST include Apache 2.0 SPDX headers.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio test --run</automated>
  </verify>
  <done>4 stub test files exist with todo tests. `pnpm --filter @calmstudio/studio test --run` passes with 0 failures (todo tests are skipped).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @calmstudio/studio test --run` exits 0
- 4 test files exist in `apps/studio/src/tests/`
- `apps/studio/playwright.config.ts` exists
- Vitest config is present in `vite.config.ts`
</verification>

<success_criteria>
Test infrastructure is fully operational: Vitest runs and passes, stub test files exist for all unit-testable modules (containment, history, clipboard, search), and Playwright is configured for future E2E tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-calm-canvas-core/02-00-SUMMARY.md`
</output>
