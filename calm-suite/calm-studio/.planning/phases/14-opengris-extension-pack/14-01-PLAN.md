---
phase: 14-opengris-extension-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/extensions/src/icons/opengris.ts
  - packages/extensions/src/packs/opengris.ts
  - packages/extensions/src/packs/opengris.test.ts
  - packages/extensions/src/index.ts
  - packages/extensions/src/registry.test.ts
  - packages/extensions/src/packs/fluxnova.test.ts
autonomous: true
requirements:
  - OGRIS-01
  - OGRIS-02
  - OGRIS-03

must_haves:
  truths:
    - "initAllPacks() registers 10 packs (was 9)"
    - "resolvePackNode('opengris:scheduler') returns a valid entry after initAllPacks()"
    - "openGrisPack has exactly 8 node types: scheduler, worker, worker-manager, client, object-storage, cluster, task-graph, parallel-function"
    - "opengris:worker-manager and opengris:cluster are containers; no other opengris node is"
    - "opengris:cluster defaultChildren includes opengris:scheduler, opengris:worker, opengris:object-storage"
    - "All 8 node icons are non-empty SVG strings"
  artifacts:
    - path: "packages/extensions/src/icons/opengris.ts"
      provides: "SVG icons for 8 OpenGRIS node types"
      contains: "opengrisIcons"
    - path: "packages/extensions/src/packs/opengris.ts"
      provides: "OpenGRIS PackDefinition with 8 nodes"
      exports: ["openGrisPack"]
    - path: "packages/extensions/src/packs/opengris.test.ts"
      provides: "Unit and integration tests for OpenGRIS pack"
      contains: "describe('openGrisPack'"
  key_links:
    - from: "packages/extensions/src/packs/opengris.ts"
      to: "packages/extensions/src/icons/opengris.ts"
      via: "import opengrisIcons"
      pattern: "import.*opengrisIcons.*from.*icons/opengris"
    - from: "packages/extensions/src/index.ts"
      to: "packages/extensions/src/packs/opengris.ts"
      via: "export and registerPack"
      pattern: "registerPack\\(openGrisPack\\)"
    - from: "packages/extensions/src/packs/opengris.test.ts"
      to: "packages/extensions/src/packs/opengris.ts"
      via: "import openGrisPack"
      pattern: "import.*openGrisPack.*from.*opengris"
---

<objective>
Add the OpenGRIS extension pack to CalmStudio, providing 8 FINOS OpenGRIS node types for distributed grid computing architecture diagrams.

Purpose: Architects modeling OpenGRIS-based distributed computing systems can use native node types (scheduler, worker, worker-manager, client, object-storage, cluster, task-graph, parallel-function) on the CalmStudio canvas with proper icons, colors, and container semantics.

Output: Three new files (icons, pack definition, tests), two modified files (index.ts registration, existing test count assertions).
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-opengris-extension-pack/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From packages/extensions/src/types.ts:
```typescript
export interface PackColor {
  bg: string;
  border: string;
  stroke: string;
  badge?: string;
}

export interface NodeTypeEntry {
  typeId: string;
  label: string;
  icon: string;
  color: PackColor;
  description?: string;
  isContainer?: boolean;
  defaultChildren?: string[];
}

export interface PackDefinition {
  id: string;
  label: string;
  version: string;
  color: PackColor;
  nodes: NodeTypeEntry[];
}
```

From packages/extensions/src/registry.ts:
```typescript
export function registerPack(pack: PackDefinition): void;
export function resolvePackNode(calmType: string): NodeTypeEntry | null;
export function getAllPacks(): PackDefinition[];
export function resetRegistry(): void;
```

From packages/extensions/src/index.ts:
```typescript
export function initAllPacks(): void;
// Currently registers 9 packs: core, fluxnova, ai, aws, gcp, azure, kubernetes, messaging, identity
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OpenGRIS icons, pack definition, and tests</name>
  <files>
    packages/extensions/src/icons/opengris.ts,
    packages/extensions/src/packs/opengris.ts,
    packages/extensions/src/packs/opengris.test.ts
  </files>
  <behavior>
    - openGrisPack.id === 'opengris'
    - openGrisPack.nodes has length 8
    - Every node has non-empty typeId, label, icon, color.bg, color.border, color.stroke, description
    - All typeIds start with 'opengris:' prefix
    - opengris:worker-manager has isContainer=true
    - opengris:cluster has isContainer=true with defaultChildren including opengris:scheduler, opengris:worker, opengris:object-storage
    - No other OpenGRIS type has isContainer=true
    - openGrisPack.color.bg === '#f0fdf4' (green family)
  </behavior>
  <action>
    Write tests FIRST in `opengris.test.ts` following the exact `fluxnova.test.ts` pattern (TDD RED phase). The test file has two describe blocks:

    1. `describe('openGrisPack')` -- unit tests for the pack definition:
       - id is 'opengris'
       - 8 node type entries
       - every node has non-empty typeId, label, icon, color (bg/border/stroke), description
       - all typeIds start with 'opengris:' prefix
       - opengris:worker-manager has isContainer=true
       - opengris:cluster has isContainer=true
       - no other OpenGRIS type has isContainer=true (filter for containers excluding worker-manager and cluster)
       - opengris:cluster defaultChildren includes opengris:scheduler
       - openGrisPack.color.bg is '#f0fdf4'

    2. `describe('OpenGRIS integration via initAllPacks')` -- skip these tests for now (they depend on Task 2's index.ts changes). Mark with `.skip` or leave out -- they will be tested in Task 2.

    After tests exist and FAIL, create the implementation:

    **`src/icons/opengris.ts`**: Export `opengrisIcons: Record<string, string>` with 8 entries. Use SPDX header. Icons are 16x16 viewBox, stroke-based, `fill="none" stroke="currentColor" stroke-width="1.2"`. Keys: scheduler, worker, worker-manager, client, object-storage, cluster, task-graph, parallel-function.

    Icon design concepts (abstract, not literal):
    - scheduler: hub with radiating spokes (central routing concept)
    - worker: rectangle with processing cross/plus (compute unit)
    - worker-manager: dashed rectangle containing two smaller rectangles (provisioning container)
    - client: monitor/terminal shape (user-facing submission)
    - object-storage: stacked cylinders or box with grid (data store)
    - cluster: large rounded rectangle with three small shapes inside (grouping)
    - task-graph: three connected circles in DAG pattern (dependency graph)
    - parallel-function: parallel vertical bars with horizontal arrow (map-reduce)

    **`src/packs/opengris.ts`**: Export `openGrisPack: PackDefinition` following the `fluxnova.ts` template exactly. Use:
    - SPDX header matching fluxnova.ts
    - `import type { PackDefinition, PackColor } from '../types.js'` (note `.js` extension)
    - `import { opengrisIcons } from '../icons/opengris.js'` (note `.js` extension)
    - `opengrisColor: PackColor` with bg '#f0fdf4', border '#16a34a', stroke '#15803d', badge '[OGRIS]'
    - Local `node()` helper function matching fluxnova.ts signature
    - Fallback icon: `opengrisIcons['scheduler']!`
    - Pack id: 'opengris' (lowercase, no hyphens -- registry splits on ':')

    The 8 nodes and descriptions from the research:
    - opengris:scheduler / Scheduler / 'Central hub that routes tasks from clients to available workers via Cap'n Proto/ZeroMQ'
    - opengris:worker / Worker / 'Executes distributed tasks assigned by the scheduler; runs on GNU/Linux'
    - opengris:worker-manager / Worker Manager / 'Provisions and terminates workers on demand via adapters (Baremetal, AWS Batch, AWS ECS, IBM Symphony)' / isContainer: true
    - opengris:client / Client / 'Submits tasks to the scheduler and retrieves results; cross-platform (Windows/Linux)'
    - opengris:object-storage / Object Storage / 'Stores serialized task arguments and results; C++ implementation for performance'
    - opengris:cluster / Cluster / 'Container grouping a full Scaler deployment (scheduler, workers, object storage)' / isContainer: true, defaultChildren: ['opengris:scheduler', 'opengris:worker', 'opengris:object-storage']
    - opengris:task-graph / Task Graph / 'DAG-based task dependency graph from opengris-pargraph; nodes are functions, edges are data dependencies'
    - opengris:parallel-function / Parallel Function / 'Function decorated with @parallel from opengris-parfun executing via map-reduce across worker pool'

    Run tests -- all pack-specific tests should now pass (GREEN phase).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/extensions test -- --reporter=verbose opengris.test 2>&1 | tail -30</automated>
  </verify>
  <done>opengris.test.ts passes: pack has id 'opengris', 8 nodes, correct typeId prefixes, two containers (worker-manager, cluster), green color family, all icons non-empty</done>
</task>

<task type="auto">
  <name>Task 2: Register OpenGRIS pack and update all count assertions</name>
  <files>
    packages/extensions/src/index.ts,
    packages/extensions/src/registry.test.ts,
    packages/extensions/src/packs/fluxnova.test.ts,
    packages/extensions/src/packs/opengris.test.ts
  </files>
  <action>
    Wire the OpenGRIS pack into the application and fix all hardcoded pack count assertions.

    **`src/index.ts`** -- Add two lines following the established pattern:
    1. Export line (after identityPack export): `export { openGrisPack } from './packs/opengris.js';`
    2. Import line (after identityPack import): `import { openGrisPack } from './packs/opengris.js';`
    3. In `initAllPacks()` body, add: `registerPack(openGrisPack);` after the identityPack line
    4. Update JSDoc comment: "Register all built-in packs (core + 9 extension packs)." (was 8)

    **`src/registry.test.ts`** line 115-117 -- Change:
    `expect(getAllPacks()).toHaveLength(9)` to `expect(getAllPacks()).toHaveLength(10)`

    **`src/packs/fluxnova.test.ts`** line 59-61 -- Change:
    `expect(getAllPacks()).toHaveLength(9)` to `expect(getAllPacks()).toHaveLength(10)`

    **`src/packs/opengris.test.ts`** -- If Task 1 left the integration describe block as `.skip`, un-skip it now and add:
    - `it('initAllPacks() registers 10 packs total')` -- calls initAllPacks(), asserts getAllPacks().toHaveLength(10)
    - `it('resolvePackNode("opengris:scheduler") returns non-null after initAllPacks()')` -- calls initAllPacks(), asserts resolvePackNode('opengris:scheduler') not null

    Run the full extensions test suite -- ALL tests must pass (including the updated count assertions in registry.test.ts and fluxnova.test.ts).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/extensions test -- --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Full extensions test suite passes: initAllPacks registers 10 packs, resolvePackNode('opengris:scheduler') returns non-null, no existing tests broken by the addition</done>
</task>

</tasks>

<verification>
Run the full extensions test suite to confirm zero regressions:
```bash
pnpm --filter @calmstudio/extensions test
```

Run typecheck to confirm strict TypeScript compliance:
```bash
pnpm --filter @calmstudio/extensions typecheck
```

Run the full project test suite to confirm no cross-package regressions:
```bash
pnpm test
```
</verification>

<success_criteria>
1. `pnpm --filter @calmstudio/extensions test` passes with zero failures
2. `pnpm typecheck` passes with zero errors
3. `openGrisPack` exports 8 node types under the `opengris:` namespace
4. `initAllPacks()` registers 10 packs (was 9)
5. `resolvePackNode('opengris:scheduler')` returns a valid entry
6. Two container nodes: `opengris:worker-manager` and `opengris:cluster`
7. All 8 SVG icons are valid non-empty strings in 16x16 stroke-based format
</success_criteria>

<output>
After completion, create `.planning/phases/14-opengris-extension-pack/14-01-SUMMARY.md`
</output>
