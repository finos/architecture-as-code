---
phase: 09-testing-suite
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/studio/src/tests/e2e/core-diagram-flow.spec.ts
  - apps/studio/src/tests/e2e/template-governance.spec.ts
  - apps/studio/src/tests/e2e/c4-navigation.spec.ts
  - apps/studio/src/tests/e2e/validation-flow.spec.ts
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "Core diagram flow E2E: user can add nodes from palette, draw edges, export CALM JSON, re-import and verify content"
    - "Template + governance E2E: user can load FluxNova template, select AI node, open governance panel, apply mitigation, export with AIGF decorator"
    - "C4 navigation E2E: user can switch Context/Container/Component views, drill into container, breadcrumb back to root"
    - "Validation flow E2E: user can create invalid architecture, click Validate, see errors, click error to select node, fix, re-validate clean"
  artifacts:
    - path: "apps/studio/src/tests/e2e/core-diagram-flow.spec.ts"
      provides: "E2E test for core diagram workflow"
      min_lines: 40
    - path: "apps/studio/src/tests/e2e/template-governance.spec.ts"
      provides: "E2E test for template + governance workflow"
      min_lines: 40
    - path: "apps/studio/src/tests/e2e/c4-navigation.spec.ts"
      provides: "E2E test for C4 view navigation"
      min_lines: 40
    - path: "apps/studio/src/tests/e2e/validation-flow.spec.ts"
      provides: "E2E test for validation workflow"
      min_lines: 40
  key_links:
    - from: "apps/studio/src/tests/e2e/core-diagram-flow.spec.ts"
      to: "localhost:5173"
      via: "page.goto + browser interactions"
      pattern: "page.goto"
    - from: "apps/studio/src/tests/e2e/template-governance.spec.ts"
      to: "localhost:5173"
      via: "page.goto + template picker + governance panel"
      pattern: "page.goto"
---

<objective>
Write 4 Playwright E2E tests covering the critical user workflows locked in CONTEXT.md: core diagram flow, template + governance, C4 navigation, and validation flow. Functional assertions only — no visual regression snapshots.

Purpose: TEST-04 requires E2E tests for full user workflows. These 4 workflows cover the entire CalmStudio feature surface. The OSFF Toronto demo flow (template + governance) becomes a regression test ensuring the demo never breaks.
Output: 4 Playwright E2E spec files testing real browser interactions against the dev server.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-suite/09-CONTEXT.md
@.planning/phases/09-testing-suite/09-RESEARCH.md
@.planning/phases/09-testing-suite/09-01-SUMMARY.md

@apps/studio/playwright.config.ts
@apps/studio/src/routes/+page.svelte
@apps/studio/src/lib/toolbar/Toolbar.svelte
@apps/studio/src/lib/canvas/CalmCanvas.svelte
@apps/studio/src/lib/palette/NodePalette.svelte
@apps/studio/src/lib/templates/TemplatePicker.svelte
@apps/studio/src/lib/governance/GovernancePanel.svelte
@apps/studio/src/lib/validation/ValidationPanel.svelte
@apps/studio/src/lib/c4/C4Breadcrumb.svelte

<interfaces>
<!-- Playwright config already exists: -->
testDir: './src/tests/e2e'
baseURL: 'http://localhost:5173'
projects: [chromium]
webServer: { command: 'pnpm dev', port: 5173, reuseExistingServer: !CI }

<!-- Key UI elements to interact with (read from component source):
- NodePalette: draggable items for each node type
- Toolbar: buttons for New, Open, Save, Export, Layout, Validate, Templates, C4 view selector
- CalmCanvas: .svelte-flow container, nodes rendered as [data-testid="flow-node-{id}"]
- TemplatePicker: full-screen modal with template cards
- GovernancePanel: right sidebar tab with risks and Apply buttons
- ValidationPanel: bottom drawer with issues list
- C4Breadcrumb: breadcrumb trail for drill navigation
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E tests for core diagram flow and template + governance flow</name>
  <files>
    apps/studio/src/tests/e2e/core-diagram-flow.spec.ts,
    apps/studio/src/tests/e2e/template-governance.spec.ts
  </files>
  <action>
    Read the following source files before writing E2E tests to understand the exact DOM structure, button labels, and data-testid attributes (if any):
    - apps/studio/src/routes/+page.svelte (page layout, toolbar/canvas/panel wiring)
    - apps/studio/src/lib/toolbar/Toolbar.svelte (button labels and IDs)
    - apps/studio/src/lib/palette/NodePalette.svelte (palette items, drag handles)
    - apps/studio/src/lib/templates/TemplatePicker.svelte (template cards, category tabs)
    - apps/studio/src/lib/governance/GovernancePanel.svelte (risk items, apply buttons)

    If components lack data-testid attributes for E2E selectors, add minimal data-testid attributes to the components as needed. Prefer getByRole/getByText selectors where possible. Only add data-testid where role/text selectors are ambiguous.

    core-diagram-flow.spec.ts:
    1. Navigate to localhost:5173
    2. Stub window.showSaveFilePicker via page.addInitScript (per RESEARCH Pitfall 3):
       ```typescript
       await page.addInitScript(() => {
         window.showSaveFilePicker = async () => ({
           createWritable: async () => ({
             write: async (data: BlobPart) => {
               (window as Record<string, unknown>)['__lastExport'] = typeof data === 'string' ? data : await new Blob([data]).text();
             },
             close: async () => {},
           }),
         }) as unknown as FileSystemFileHandle;
       });
       ```
    3. Add a node from palette (click-to-place or drag from palette — read NodePalette to determine the interaction pattern)
    4. Add a second node
    5. Draw an edge between them (if possible via keyboard/click interaction — otherwise verify nodes exist)
    6. Trigger Export > CALM JSON
    7. Read exported content from window.__lastExport via page.evaluate
    8. Verify exported JSON has correct node count and relationship structure
    9. Test re-import: stub showOpenFilePicker with the exported JSON content, trigger Open, verify nodes appear

    template-governance.spec.ts:
    1. Navigate to localhost:5173
    2. Stub FSA API (same pattern)
    3. Open template picker (click Templates button in toolbar)
    4. Verify template picker modal is visible
    5. Select a FluxNova template (e.g., "FluxNova Platform" or first available)
    6. Verify nodes appear on canvas (check for node elements in DOM)
    7. Select an AI node (if the template has one) or load the AI Agent template
    8. Verify governance panel shows applicable risks
    9. Click "Apply" on a mitigation
    10. Trigger Export > CALM JSON
    11. Verify exported JSON contains AIGF decorator (or controls on the node)

    Use Playwright test() and expect() from @playwright/test.
    Add `test.setTimeout(60000)` for each test to allow dev server startup time.
    No visual regression snapshots per CONTEXT.md decision.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio/apps/studio && npx playwright test src/tests/e2e/core-diagram-flow.spec.ts src/tests/e2e/template-governance.spec.ts --reporter=list</automated>
  </verify>
  <done>core-diagram-flow.spec.ts tests the full create-add-export-reimport cycle. template-governance.spec.ts tests template loading, governance interaction, and AIGF export. Both pass against the dev server.</done>
</task>

<task type="auto">
  <name>Task 2: E2E tests for C4 navigation flow and validation flow</name>
  <files>
    apps/studio/src/tests/e2e/c4-navigation.spec.ts,
    apps/studio/src/tests/e2e/validation-flow.spec.ts
  </files>
  <action>
    Read the following source files before writing E2E tests:
    - apps/studio/src/lib/c4/C4Breadcrumb.svelte (breadcrumb structure)
    - apps/studio/src/lib/validation/ValidationPanel.svelte (issue list structure)
    - apps/studio/src/lib/toolbar/Toolbar.svelte (C4 view selector, Validate button)

    c4-navigation.spec.ts:
    1. Navigate to localhost:5173
    2. First, create an architecture with container nodes (either via template or programmatic setup):
       - Load a template that has containment relationships (e.g., FluxNova Platform has fluxnova:platform container)
       - Or add nodes and containment edges manually
    3. Find and click the C4 view selector in toolbar — switch to "Context" view
    4. Verify the view changes (check for C4 level indicator or filtered node count)
    5. Switch to "Container" view
    6. Double-click a system/container node to drill down
    7. Verify breadcrumb appears with navigation trail
    8. Click breadcrumb root to return to top level
    9. Verify breadcrumb disappears and full view is restored
    10. Exit C4 mode (click "All" or toggle off)
    11. Verify canvas returns to normal editing mode

    validation-flow.spec.ts:
    1. Navigate to localhost:5173
    2. Create an invalid architecture:
       - Add a node from palette (it should have auto-generated IDs which may be valid)
       - To create a validation error, use the code panel or properties panel to clear a required field (e.g., delete the node name or set node-type to empty)
       - Or add a node and leave it orphaned (no relationships)
    3. Click Validate button in toolbar
    4. Verify validation panel opens
    5. Verify error/warning items appear in the panel
    6. Click on an error item — verify the corresponding node gets selected on canvas (or the properties panel shows the node)
    7. Fix the issue (e.g., add the missing field back via properties panel)
    8. Click Validate again
    9. Verify the issue is resolved (fewer issues or clean validation)

    Use test.setTimeout(60000) for startup time.
    No visual regression snapshots.
    Prefer role/text selectors over data-testid. Add data-testid to components only where selectors are ambiguous (reuse any added in Task 1).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio/apps/studio && npx playwright test src/tests/e2e/c4-navigation.spec.ts src/tests/e2e/validation-flow.spec.ts --reporter=list</automated>
  </verify>
  <done>c4-navigation.spec.ts tests view switching, drill-down, breadcrumb navigation, and mode exit. validation-flow.spec.ts tests error detection, panel display, error-to-node navigation, and re-validation after fix. Both pass against the dev server.</done>
</task>

</tasks>

<verification>
1. `cd apps/studio && npx playwright test --reporter=list` runs all 4 E2E specs and they pass
2. Each spec covers one of the 4 locked workflows from CONTEXT.md
3. Tests use functional assertions only — no screenshot comparisons
4. File I/O is mocked at browser level via page.addInitScript (RESEARCH Pitfall 3)
5. Tests complete within 2 minutes each (including dev server startup)
</verification>

<success_criteria>
- 4 Playwright E2E spec files in apps/studio/src/tests/e2e/
- All 4 CONTEXT.md-locked workflows covered
- Functional assertions only (no visual regression)
- File I/O mocked at browser level
- All specs pass against Chromium via `pnpm test:e2e`
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-suite/09-04-SUMMARY.md`
</output>
