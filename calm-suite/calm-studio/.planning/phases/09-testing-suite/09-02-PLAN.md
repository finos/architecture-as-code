---
phase: 09-testing-suite
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/studio/src/tests/stores/validation.test.ts
  - apps/studio/src/tests/stores/governance.test.ts
  - apps/studio/src/tests/stores/c4State.test.ts
  - apps/studio/src/tests/io/export.test.ts
  - apps/studio/src/tests/templates/registry.test.ts
  - apps/studio/src/tests/integration/sync-integration.test.ts
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "validation.svelte.ts store has unit tests covering runValidation, clearValidation, getIssues, getIssuesForNode, getErrorCountForNode, isPanelOpen, selectNodeFromIssue"
    - "governance.svelte.ts store has unit tests covering refreshGovernance, getArchitectureScore, hasAINodes, getSelectedNodeGovernance, updateSelectedNodeGovernance"
    - "c4State.svelte.ts store has unit tests covering enterC4Mode, exitC4Mode, setC4Level, drillDown, drillUpTo, resetC4State"
    - "export.ts has unit tests covering generateAIGFDecorator and exportAsCalm (CALM JSON content verification, not file I/O)"
    - "templates/registry.ts has unit tests covering registerTemplate, loadTemplate, getTemplatesByCategory, getCategories, getAllTemplates, initAllTemplates"
    - "Bidirectional sync integration test verifies applyFromJson -> getModel -> applyFromCanvas round-trip preserves data"
  artifacts:
    - path: "apps/studio/src/tests/stores/validation.test.ts"
      provides: "Validation store unit tests"
      min_lines: 60
    - path: "apps/studio/src/tests/stores/governance.test.ts"
      provides: "Governance store unit tests"
      min_lines: 60
    - path: "apps/studio/src/tests/stores/c4State.test.ts"
      provides: "C4 state store unit tests"
      min_lines: 80
    - path: "apps/studio/src/tests/io/export.test.ts"
      provides: "Export module unit tests"
      min_lines: 50
    - path: "apps/studio/src/tests/templates/registry.test.ts"
      provides: "Template registry unit tests"
      min_lines: 50
    - path: "apps/studio/src/tests/integration/sync-integration.test.ts"
      provides: "Bidirectional sync integration test"
      min_lines: 40
  key_links:
    - from: "apps/studio/src/tests/stores/governance.test.ts"
      to: "$lib/stores/calmModel.svelte"
      via: "applyFromJson setup before governance calls"
      pattern: "applyFromJson.*createAIGovernanceArch"
    - from: "apps/studio/src/tests/stores/validation.test.ts"
      to: "$lib/stores/calmModel.svelte"
      via: "applyFromJson setup before validation calls"
      pattern: "applyFromJson"
    - from: "apps/studio/src/tests/integration/sync-integration.test.ts"
      to: "$lib/stores/projection"
      via: "applyFromCanvas round-trip"
      pattern: "applyFromCanvas|projectToCanvas"
---

<objective>
Write unit tests for the 5 untested store/module modules (validation.svelte.ts, governance.svelte.ts, c4State.svelte.ts, export.ts, templates/registry.ts) and a bidirectional sync integration test. These fill the critical coverage gaps identified in CONTEXT.md.

Purpose: TEST-02 requires unit tests for sync engine, CALM model, validation, and C4 filtering. TEST-03 requires integration tests for bidirectional sync. This plan covers all untested modules.
Output: 6 new test files covering all untested stores and the sync integration round-trip.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-suite/09-CONTEXT.md
@.planning/phases/09-testing-suite/09-RESEARCH.md
@.planning/phases/09-testing-suite/09-01-SUMMARY.md

@apps/studio/src/lib/stores/validation.svelte.ts
@apps/studio/src/lib/stores/governance.svelte.ts
@apps/studio/src/lib/c4/c4State.svelte.ts
@apps/studio/src/lib/io/export.ts
@apps/studio/src/lib/templates/registry.ts
@apps/studio/src/lib/stores/calmModel.svelte.ts
@apps/studio/src/lib/stores/projection.ts
@apps/studio/src/tests/calmModel.test.ts
@apps/studio/src/tests/projection.test.ts
@packages/calm-core/test-fixtures/index.ts

<interfaces>
<!-- Exports from validation.svelte.ts that need testing -->
From apps/studio/src/lib/stores/validation.svelte.ts:
- export type { ValidationIssue }
- export function runValidation(): void
- export function clearValidation(): void
- export function getIssues(): ValidationIssue[]
- export function getIssuesForNode(nodeId: string): ValidationIssue[]
- export function getErrorCountForNode(nodeId: string): number
- export function isPanelOpen(): boolean
- export function closePanel(): void (inferred)
- export function selectNodeFromIssue(nodeId: string | null): void (inferred)
- export function getSelectedIssueNodeId(): string | null

<!-- Exports from governance.svelte.ts that need testing -->
From apps/studio/src/lib/stores/governance.svelte.ts:
- export function getArchitectureScore(): number | null
- export function hasAINodes(): boolean
- export function getSelectedNodeGovernance(): { risks, mitigations, appliedControls }
- export function refreshGovernance(): void
- export function updateSelectedNodeGovernance(nodeId: string, ...): void

<!-- Exports from c4State.svelte.ts that need testing -->
From apps/studio/src/lib/c4/c4State.svelte.ts:
- export type DrillEntry
- export function isC4Mode(): boolean
- export function getC4Level(): C4Level | null
- export function getC4DrillStack(): DrillEntry[]
- export function getCurrentDrillParentId(): string | null
- export function enterC4Mode(level: C4Level): void
- export function setC4Level(level: C4Level): void
- export function exitC4Mode(): void
- export function drillDown(nodeId: string, label: string): void
- export function drillUpTo(index: number): void
- export function resetC4State(): void

<!-- Exports from export.ts that need testing -->
From apps/studio/src/lib/io/export.ts:
- export function generateAIGFDecorator(calmJsonStr: string): object | null (inferred from AIGF decorator)
- export function exportAsCalm(calmJsonStr: string, filename?: string): void

<!-- Exports from templates/registry.ts that need testing -->
From apps/studio/src/lib/templates/registry.ts:
- export interface CalmTemplateMeta
- export interface CalmTemplate extends CalmArchitecture
- export function registerTemplate(template: CalmTemplate): void
- export function loadTemplate(id: string): CalmArchitecture
- export function getTemplatesByCategory(category: string): CalmTemplate[]
- export function getCategories(): string[]
- export function getAllTemplates(): CalmTemplate[]
- export function initAllTemplates(): void
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for validation, governance, and c4State stores</name>
  <files>
    apps/studio/src/tests/stores/validation.test.ts,
    apps/studio/src/tests/stores/governance.test.ts,
    apps/studio/src/tests/stores/c4State.test.ts
  </files>
  <behavior>
    validation.test.ts:
    - runValidation on empty arch returns no errors, opens panel
    - runValidation on arch with unnamed node returns error issues
    - clearValidation empties issues and closes panel
    - getIssuesForNode returns only issues for specified node ID
    - getErrorCountForNode returns correct count
    - selectNodeFromIssue sets selectedIssueNodeId; getSelectedIssueNodeId returns it
    - isPanelOpen returns true after runValidation, false after closePanel

    governance.test.ts:
    - refreshGovernance with no AI nodes: hasAINodes returns false, score is null
    - refreshGovernance with AI nodes (ai:llm, ai:agent): hasAINodes returns true, score is a number
    - getSelectedNodeGovernance returns risks/mitigations for ai:llm node
    - getArchitectureScore returns number between 0-100 when AI nodes present
    - updateSelectedNodeGovernance updates controls on specified node

    c4State.test.ts:
    - Initial state: isC4Mode false, level null, drill stack empty
    - enterC4Mode('context'): isC4Mode true, level 'context', stack cleared
    - setC4Level changes level without affecting drill stack
    - drillDown pushes entry onto stack, getCurrentDrillParentId returns nodeId
    - drillUpTo(0) clears stack to root
    - drillUpTo(1) with 3-deep stack keeps first entry only
    - exitC4Mode resets all state (mode false, level null, stack empty)
    - resetC4State resets all state
  </behavior>
  <action>
    Create `apps/studio/src/tests/stores/` directory.

    Write validation.test.ts:
    - Import from $lib/stores/validation.svelte and $lib/stores/calmModel.svelte
    - Import createMinimalArch from @calmstudio/calm-core/test-fixtures
    - beforeEach: resetModel() + clearValidation()
    - Test all behaviors listed above
    - Follow existing test patterns: describe/it/expect, direct function calls, getter assertions
    - Per RESEARCH Pitfall 6: call applyFromJson() before runValidation() so getModel() returns known data

    Write governance.test.ts:
    - Import from $lib/stores/governance.svelte and $lib/stores/calmModel.svelte
    - Import createAIGovernanceArch, createMinimalArch from @calmstudio/calm-core/test-fixtures
    - beforeEach: resetModel() + reset governance (if reset function exists; otherwise clear by calling refreshGovernance with empty arch)
    - Per RESEARCH Pitfall 6: applyFromJson(createAIGovernanceArch()) in beforeEach before governance function calls
    - Test all governance behaviors: score calculation, AI node detection, per-node governance data
    - Read governance.svelte.ts source first to understand exact export signatures and reset pattern

    Write c4State.test.ts:
    - Import from $lib/c4/c4State.svelte
    - beforeEach: resetC4State()
    - Test all C4 state transitions listed in behaviors
    - Follow pattern from existing c4Filter.test.ts for test structure
    - No calmModel dependency needed — c4State is self-contained state management
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio run test</automated>
  </verify>
  <done>validation.test.ts has 7+ test cases covering all validation store exports. governance.test.ts has 5+ test cases covering score, AI detection, per-node governance. c4State.test.ts has 8+ test cases covering all state transitions. All pass alongside existing 11 test suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for export module, template registry, and sync integration</name>
  <files>
    apps/studio/src/tests/io/export.test.ts,
    apps/studio/src/tests/templates/registry.test.ts,
    apps/studio/src/tests/integration/sync-integration.test.ts
  </files>
  <behavior>
    export.test.ts:
    - generateAIGFDecorator with AI architecture returns decorator object with risks and score
    - generateAIGFDecorator with non-AI architecture returns null
    - exportAsCalm strips _template metadata from output JSON
    - exportAsCalm includes AIGF decorator when AI nodes present
    - exportAsCalm preserves all CALM fields (nodes, relationships, controls)
    NOTE: SVG/PNG export requires real browser DOM — skip those. Focus on pure function logic.

    registry.test.ts:
    - registerTemplate adds template to registry
    - loadTemplate returns architecture for known template ID (without _template metadata)
    - loadTemplate throws for unknown template ID
    - getTemplatesByCategory returns templates matching category
    - getCategories returns unique category list
    - getAllTemplates returns all registered templates
    - initAllTemplates registers all 6 FluxNova templates (test count after init)

    sync-integration.test.ts:
    - applyFromJson with 2-node architecture, projectToCanvas produces matching nodes/edges, applyFromCanvas round-trip preserves data
    - Adding a node via addNode, then getModel contains the new node in CALM JSON
    - Editing a property via mutation function, then projectToCanvas reflects the change
  </behavior>
  <action>
    Create `apps/studio/src/tests/io/` directory.
    Create `apps/studio/src/tests/templates/` directory.
    Create `apps/studio/src/tests/integration/` directory.

    Write export.test.ts:
    - Import generateAIGFDecorator (or the decorator generation logic) and exportAsCalm from $lib/io/export
    - Read export.ts source first to understand exact function signatures — the export functions may use DOM APIs (Blob, createObjectURL). For unit testing, test generateAIGFDecorator as a pure function. For exportAsCalm, test the JSON content transformation (strip _template, add decorator) by mocking Blob/URL APIs via vi.stubGlobal or testing the content string directly.
    - Import createAIGovernanceArch, createMinimalArch from @calmstudio/calm-core/test-fixtures
    - Mock window.showSaveFilePicker and Blob via vi.stubGlobal for exportAsCalm tests (or extract the content preparation logic and test that directly)
    - beforeEach: resetModel() + applyFromJson() with appropriate fixture

    Write registry.test.ts:
    - Import from $lib/templates/registry
    - Test template registration, retrieval, category filtering
    - Call initAllTemplates() and verify all 6 FluxNova templates are registered
    - loadTemplate should return clean CalmArchitecture (no _template field)
    - Test error case: loadTemplate with unknown ID

    Write sync-integration.test.ts:
    - Import from $lib/stores/calmModel.svelte, $lib/stores/projection
    - Import createMinimalArch from @calmstudio/calm-core/test-fixtures
    - beforeEach: resetModel()
    - Round-trip test: applyFromJson(arch) -> projectToCanvas() -> verify nodes match -> applyFromCanvas(nodes, edges) -> getModel() -> verify CALM JSON matches original
    - Read existing projection.test.ts to understand the projectToCanvas/applyFromCanvas function signatures
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio run test</automated>
  </verify>
  <done>export.test.ts has 5+ test cases covering AIGF decorator generation and CALM JSON export content. registry.test.ts has 7+ test cases covering full template lifecycle. sync-integration.test.ts has 3+ test cases verifying bidirectional sync round-trip. All pass alongside existing test suites.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @calmstudio/studio run test` passes with all new + existing tests (17+ test suites total)
2. New test files follow established patterns (describe/it/expect, beforeEach reset, direct function calls)
3. Each untested module now has dedicated test coverage
4. Sync integration test proves round-trip data preservation
</verification>

<success_criteria>
- 6 new test files created in apps/studio/src/tests/
- All 5 untested modules have dedicated test coverage
- Bidirectional sync integration test verifies data round-trip
- All tests pass in under 30 seconds per TEST-01 requirement
- Existing 11 test suites unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-suite/09-02-SUMMARY.md`
</output>
