---
phase: 09-testing-suite
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/studio/src/tests/components/NodeProperties.test.ts
  - apps/studio/src/tests/components/EdgeProperties.test.ts
  - apps/studio/src/tests/components/ControlsList.test.ts
  - apps/studio/src/tests/components/GovernancePanel.test.ts
  - apps/studio/src/tests/components/TemplatePicker.test.ts
  - apps/studio/src/tests/components/ValidationPanel.test.ts
  - apps/studio/src/tests/components/Toolbar.test.ts
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "Each of the 7 interactive panel components renders with real data and produces correct DOM output"
    - "Component tests use @testing-library/svelte render() + fireEvent — no mocked stores"
    - "Basic a11y checks pass: ARIA labels exist, buttons focusable, form inputs labeled"
    - "Component tests treat Svelte 5 runes as implementation details — assert rendered output only"
  artifacts:
    - path: "apps/studio/src/tests/components/NodeProperties.test.ts"
      provides: "NodeProperties component tests"
      min_lines: 40
    - path: "apps/studio/src/tests/components/EdgeProperties.test.ts"
      provides: "EdgeProperties component tests"
      min_lines: 40
    - path: "apps/studio/src/tests/components/ControlsList.test.ts"
      provides: "ControlsList component tests"
      min_lines: 30
    - path: "apps/studio/src/tests/components/GovernancePanel.test.ts"
      provides: "GovernancePanel component tests"
      min_lines: 40
    - path: "apps/studio/src/tests/components/TemplatePicker.test.ts"
      provides: "TemplatePicker component tests"
      min_lines: 30
    - path: "apps/studio/src/tests/components/ValidationPanel.test.ts"
      provides: "ValidationPanel component tests"
      min_lines: 30
    - path: "apps/studio/src/tests/components/Toolbar.test.ts"
      provides: "Toolbar component tests"
      min_lines: 30
  key_links:
    - from: "apps/studio/src/tests/components/NodeProperties.test.ts"
      to: "$lib/properties/NodeProperties.svelte"
      via: "render(NodeProperties, { props })"
      pattern: "render.*NodeProperties"
    - from: "apps/studio/src/tests/components/GovernancePanel.test.ts"
      to: "$lib/stores/calmModel.svelte"
      via: "applyFromJson setup before render"
      pattern: "applyFromJson.*createAIGovernanceArch"
---

<objective>
Write @testing-library/svelte component tests for the 7 interactive panel components: NodeProperties, EdgeProperties, ControlsList, GovernancePanel, TemplatePicker, ValidationPanel, and Toolbar. Tests use real data with fixtures, not mocked stores.

Purpose: TEST-05 requires component-level tests for all custom Svelte components. CONTEXT.md locked the scope to 7 interactive panels (skip pure-display node/edge components). Tests include basic a11y assertions.
Output: 7 component test files testing rendered DOM output and user interactions.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-suite/09-CONTEXT.md
@.planning/phases/09-testing-suite/09-RESEARCH.md
@.planning/phases/09-testing-suite/09-01-SUMMARY.md

@apps/studio/src/lib/properties/NodeProperties.svelte
@apps/studio/src/lib/properties/EdgeProperties.svelte
@apps/studio/src/lib/properties/ControlsList.svelte
@apps/studio/src/lib/governance/GovernancePanel.svelte
@apps/studio/src/lib/templates/TemplatePicker.svelte
@apps/studio/src/lib/validation/ValidationPanel.svelte
@apps/studio/src/lib/toolbar/Toolbar.svelte
@apps/studio/src/lib/stores/calmModel.svelte.ts
@apps/studio/src/lib/stores/validation.svelte.ts
@apps/studio/src/lib/stores/governance.svelte.ts
@packages/calm-core/test-fixtures/index.ts

<interfaces>
<!-- Component prop interfaces must be read from source. Key patterns: -->

NodeProperties.svelte expects:
- props: { node: Node (Svelte Flow), onBeforeFirstEdit?: () => void, onmutate?: () => void }
- Reads from calmModel store via getNodeCalm(nodeId) to get CALM metadata

EdgeProperties.svelte expects:
- props: { edge: Edge (Svelte Flow), onBeforeFirstEdit?: () => void, onmutate?: () => void }
- Reads from calmModel store via getRelationshipCalm(edgeId)

ControlsList.svelte expects:
- props: { controls: CalmControls, onchange?: (controls: CalmControls) => void }

GovernancePanel.svelte expects:
- Reads from governance store (getSelectedNodeGovernance, getArchitectureScore)
- Reads selected node from calmModel store

TemplatePicker.svelte expects:
- props: { onselect: (template: CalmArchitecture) => void, onclose: () => void }
- Reads from templates registry (getAllTemplates, getCategories)

ValidationPanel.svelte expects:
- Reads from validation store (getIssues, isPanelOpen)

Toolbar.svelte expects:
- Multiple callback props for file operations, layout, C4 mode, etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Component tests for NodeProperties, EdgeProperties, ControlsList, and ValidationPanel</name>
  <files>
    apps/studio/src/tests/components/NodeProperties.test.ts,
    apps/studio/src/tests/components/EdgeProperties.test.ts,
    apps/studio/src/tests/components/ControlsList.test.ts,
    apps/studio/src/tests/components/ValidationPanel.test.ts
  </files>
  <behavior>
    NodeProperties.test.ts:
    - Renders node name in an input field when node is selected
    - Name input has associated label (a11y: getByLabelText or getByRole)
    - Renders node-type display
    - Renders description field
    - All text inputs are focusable

    EdgeProperties.test.ts:
    - Renders relationship type when edge is selected
    - Renders source and destination fields
    - Protocol field renders for 'connects' type edges
    - Form inputs have associated labels (a11y)

    ControlsList.test.ts:
    - Renders controls section when controls object provided
    - Shows empty state when controls is empty object
    - Control domain entries render with correct keys

    ValidationPanel.test.ts:
    - Renders issues list when validation has been run
    - Shows error/warning/info severity indicators
    - Shows empty state when no issues
    - Issue items are clickable (buttons/links for navigation)
  </behavior>
  <action>
    Create `apps/studio/src/tests/components/` directory.

    IMPORTANT: Read each component's source file before writing tests to understand:
    1. Exact prop interface (Svelte 5 uses `let { prop1, prop2 } = $props()` pattern)
    2. What store state needs to be pre-populated
    3. What DOM elements are rendered

    For all component tests:
    - Import { render, fireEvent } from '@testing-library/svelte'
    - Import { describe, it, expect, beforeEach } from 'vitest'
    - Import store reset/setup functions from $lib/stores/calmModel.svelte
    - Import fixtures from @calmstudio/calm-core/test-fixtures
    - beforeEach: resetModel() + applyFromJson() with appropriate fixture
    - Use getByRole, getByLabelText, getByText for queries (accessibility-first)
    - Include at least 1 a11y assertion per component (ARIA labels, focusable buttons)

    NodeProperties.test.ts:
    - Create Svelte Flow Node object matching the fixture data: { id: 'svc-1', type: 'service', position: {x:0,y:0}, data: { label: 'API Service', calmId: 'svc-1', calmType: 'service', description: 'Backend API' } }
    - render(NodeProperties, { props: { node: flowNode } })
    - Assert input values match CALM model data

    EdgeProperties.test.ts:
    - Create Svelte Flow Edge object matching fixture relationship
    - render(EdgeProperties, { props: { edge: flowEdge } })

    ControlsList.test.ts:
    - Create controls object with a security domain entry
    - render(ControlsList, { props: { controls: controlsObj } })

    ValidationPanel.test.ts:
    - Pre-populate model with an invalid architecture (node without required fields)
    - Call runValidation() from validation store
    - render(ValidationPanel) — reads from validation store directly
    - Assert issues are rendered in the DOM

    If @testing-library/svelte render() fails for Svelte 5 components, check RESEARCH Pitfall 2. The installed version (5.3.1) should auto-detect Svelte 5. If issues arise, try importing from '@testing-library/svelte/svelte5' or check peer dependency resolution.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio run test</automated>
  </verify>
  <done>4 component test files created. NodeProperties tests verify name/type/description rendering + a11y labels. EdgeProperties tests verify relationship fields. ControlsList tests verify domain rendering. ValidationPanel tests verify issues list rendering. All pass alongside existing suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests for GovernancePanel, TemplatePicker, and Toolbar</name>
  <files>
    apps/studio/src/tests/components/GovernancePanel.test.ts,
    apps/studio/src/tests/components/TemplatePicker.test.ts,
    apps/studio/src/tests/components/Toolbar.test.ts
  </files>
  <behavior>
    GovernancePanel.test.ts:
    - Renders governance score when AI nodes present in model
    - Shows applicable risks for selected AI node
    - Shows "No AI nodes" or equivalent when architecture has no AI nodes
    - Apply mitigation button is rendered and focusable (a11y)

    TemplatePicker.test.ts:
    - Renders template cards/list for all registered templates
    - Category tabs/filters render unique categories
    - Calls onselect callback when template is clicked
    - Calls onclose callback when close button is clicked
    - Close button is focusable and has accessible name (a11y)

    Toolbar.test.ts:
    - Renders core toolbar buttons (New, Open, Save, Export, Layout, Validate, Templates)
    - Buttons have accessible names (getByRole('button', { name: /.../ }))
    - Clicking button calls corresponding callback prop
    - Governance badge visible when AI nodes present in model
  </behavior>
  <action>
    Read each component's source file before writing tests.

    GovernancePanel.test.ts:
    - Import governance store functions + calmModel store
    - beforeEach: resetModel() + applyFromJson(createAIGovernanceArch()) + refreshGovernance()
    - render(GovernancePanel) — the component reads from governance store
    - Read GovernancePanel.svelte to understand its exact props interface (it may take selectedNodeId or read from a store)
    - Assert score display, risk items, mitigation buttons

    TemplatePicker.test.ts:
    - Call initAllTemplates() before render to populate registry
    - render(TemplatePicker, { props: { onselect: vi.fn(), onclose: vi.fn() } })
    - Assert template cards render (check for template names in DOM)
    - fireEvent.click on a template card, assert onselect was called
    - fireEvent.click on close button, assert onclose was called

    Toolbar.test.ts:
    - Toolbar has many callback props. Read source to identify required props.
    - Create vi.fn() mocks for each callback prop
    - render(Toolbar, { props: { onNew: vi.fn(), onOpen: vi.fn(), onSave: vi.fn(), ... } })
    - Assert buttons render with accessible names
    - Click a button, assert callback was invoked
    - For governance badge: pre-populate model with AI arch, call refreshGovernance()

    a11y: For each component, verify at least one element via getByRole with accessible name.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio run test</automated>
  </verify>
  <done>3 component test files created. GovernancePanel tests verify score display and risk rendering with AI nodes. TemplatePicker tests verify template listing and callback invocation. Toolbar tests verify button rendering and accessibility. All 7 component test files pass alongside existing suites.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @calmstudio/studio run test` passes with all 7 new component test files
2. Each component test uses @testing-library/svelte render() + fireEvent (not direct DOM manipulation)
3. Each component test includes at least 1 a11y assertion (getByRole, getByLabelText)
4. No store mocking — tests use real stores with fixture data
5. Svelte 5 runes are never directly asserted — only rendered DOM output
</verification>

<success_criteria>
- 7 component test files in apps/studio/src/tests/components/
- All 7 interactive panels have test coverage
- Tests follow @testing-library/svelte patterns per RESEARCH Pattern 4
- Basic a11y checks pass for each component
- All tests pass in under 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-suite/09-03-SUMMARY.md`
</output>
