---
phase: 04-import-export-layout
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified: []
autonomous: false
requirements: [IOEX-01, IOEX-02, IOEX-03, IOEX-04, IOEX-05, IOEX-06, LAYT-01, LAYT-02, LAYT-03]

must_haves:
  truths:
    - "All Phase 4 success criteria verified by human in running application"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 4 deliverable by the user.

Purpose: Confirm that import, export, layout, and file I/O all work correctly in the running application before marking Phase 4 complete.
Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-import-export-layout/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and prepare test CALM JSON file</name>
  <files>/tmp/test-architecture.calm.json (runtime-created, not tracked)</files>
  <action>
1. Start the dev server: `pnpm --filter @calmstudio/studio dev` (background)
2. Create a test CALM JSON file at `/tmp/test-architecture.calm.json` with a representative 4-node architecture:
   - An actor "User"
   - A system "API Gateway"
   - A service "Order Service"
   - A database "Orders DB"
   - Three relationships: User -> API Gateway (connects/HTTPS), API Gateway -> Order Service (interacts), Order Service -> Orders DB (connects/JDBC)
3. Verify dev server is running and accessible at localhost

No project source files are modified by this task.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:5173</automated>
  </verify>
  <done>Dev server running. Test CALM JSON file created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 4</name>
  <files></files>
  <action>
Present the running application to the user for manual verification of all Phase 4 features. The user will test import, export, layout, file I/O, and keyboard shortcuts.
  </action>
  <verify>
Visit http://localhost:5173 and test the following:

1. Import (IOEX-01): Drag `/tmp/test-architecture.calm.json` onto the canvas. Verify 4 nodes appear auto-laid out with edges visible, fitView zooms to show all nodes.
2. Open via Cmd+O (IOEX-06): Press Cmd+O, select test file. Same result as drag-and-drop.
3. Auto-layout (LAYT-01, LAYT-03): Click auto-layout button. Nodes rearrange cleanly. Try "Left to Right" from dropdown.
4. Pin toggle (LAYT-02): Hover node, click pin icon. Auto-layout again. Pinned node stays in place.
5. Save (IOEX-06): Move a node, verify dirty dot appears. Cmd+S, verify save. Dirty indicator disappears.
6. Export CALM JSON (IOEX-02): Export > CALM JSON. Valid .calm.json downloaded.
7. Export SVG (IOEX-04): Export > SVG. .svg file opens in browser with transparent background.
8. Export PNG (IOEX-05): Export > PNG. .png file renders diagram clearly.
9. Export calmscript (IOEX-03): Export > calmscript. File downloads (may be stub).
10. New (Cmd+N): With unsaved changes, Cmd+N. Prompted to save. Canvas clears.
11. beforeunload: Make changes, close tab. Browser shows save dialog.
12. Error banner: Drop an invalid JSON file (e.g., a .json file with `{"foo": "bar"}` — no nodes array). Verify error banner appears above canvas. Click X to dismiss. Canvas unchanged.
  </verify>
  <done>Human approves all Phase 4 features work correctly. Type "approved" or describe issues found.</done>
</task>

</tasks>

<verification>
All 12 verification steps above pass human inspection.
</verification>

<success_criteria>
- Human approves all Phase 4 features work correctly in the running application
- No critical bugs or broken workflows identified
</success_criteria>

<output>
After completion, create `.planning/phases/04-import-export-layout/04-04-SUMMARY.md`
</output>
