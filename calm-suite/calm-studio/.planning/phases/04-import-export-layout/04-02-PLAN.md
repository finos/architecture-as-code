---
phase: 04-import-export-layout
plan: 02
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - apps/studio/src/lib/io/fileSystem.ts
  - apps/studio/src/lib/io/fileState.svelte.ts
  - apps/studio/src/lib/io/export.ts
autonomous: true
requirements: [IOEX-02, IOEX-03, IOEX-04, IOEX-05, IOEX-06]

must_haves:
  truths:
    - "User can save a diagram via Cmd+S and reload it later with all data preserved"
    - "File System Access API used on Chrome/Edge; Blob download fallback on Firefox/Safari"
    - "Dirty state tracked: dot indicator shown, beforeunload prompt on tab close"
    - "User can export diagram as CALM JSON file"
  artifacts:
    - path: "apps/studio/src/lib/io/fileSystem.ts"
      provides: "openFile, saveFile, downloadDataUrl functions"
      exports: ["openFile", "saveFile", "saveFileAs", "downloadDataUrl"]
      min_lines: 40
    - path: "apps/studio/src/lib/io/fileState.svelte.ts"
      provides: "File state store (filename, handle, dirty flag)"
      exports: ["getFileName", "getFileHandle", "getIsDirty", "markDirty", "markClean", "resetFileState"]
      min_lines: 20
    - path: "apps/studio/src/lib/io/export.ts"
      provides: "CALM JSON, SVG, PNG, calmscript export functions"
      exports: ["exportAsCalm", "exportAsSvg", "exportAsPng", "exportAsCalmscript"]
      min_lines: 10
  key_links:
    - from: "apps/studio/src/lib/io/fileSystem.ts"
      to: "window.showOpenFilePicker"
      via: "feature detection"
      pattern: "showOpenFilePicker.*in.*window"
    - from: "apps/studio/src/lib/io/fileState.svelte.ts"
      to: "apps/studio/src/lib/io/fileSystem.ts"
      via: "markClean after save"
      pattern: "markClean"
---

<objective>
Implement file I/O (open, save, save-as) with File System Access API + fallback, dirty state tracking store, and all export functions (CALM JSON, SVG, PNG, calmscript).

Purpose: File persistence is the foundation for all export formats and the save/load workflow.
Output: Three modules (fileSystem.ts, fileState.svelte.ts, export.ts) providing complete file I/O and export.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-import-export-layout/04-RESEARCH.md
@.planning/phases/04-import-export-layout/04-00-SUMMARY.md

@apps/studio/src/lib/stores/calmModel.svelte.ts
@apps/studio/src/tests/fileSystem.test.ts

<interfaces>
From apps/studio/src/lib/stores/calmModel.svelte.ts:
```typescript
export function getModelJson(): string;  // Returns pretty-printed CALM JSON
export function getModel(): CalmArchitecture;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement file system module and file state store</name>
  <files>apps/studio/src/lib/io/fileSystem.ts, apps/studio/src/lib/io/fileState.svelte.ts, apps/studio/src/tests/fileSystem.test.ts</files>
  <behavior>
    - openFile fallback: creates input element, reads file content, returns {content, name, handle: null}
    - saveFile with handle: calls handle.createWritable(), writes, closes
    - saveFile without handle and no showSaveFilePicker: triggers Blob download
    - downloadDataUrl: creates anchor with href=dataUrl, download=filename, clicks it
  </behavior>
  <action>
1. Create `apps/studio/src/lib/io/fileSystem.ts`:
   - `openFile()`: Feature-detect `showOpenFilePicker` in window. If available, use it with types filter for `.json` files. If not, create hidden `<input type="file">` with accept=`.json,.calm.json`, trigger click, read file content. Return `{ content: string, name: string, handle: FileSystemFileHandle | null }`.
   - `saveFile(content, handle, filename)`: If handle provided, use `handle.createWritable()` flow. Else if `showSaveFilePicker` in window, use it with suggestedName. Else use Blob download fallback. Return the handle (new or existing) or null.
   - `saveFileAs(content, filename)`: Always prompts — use `showSaveFilePicker` if available, else Blob download. Return handle or null.
   - `downloadDataUrl(dataUrl, filename)`: Create anchor, set href + download, click, no revoke needed (data URL).
   - Per RESEARCH Pitfall 5: All picker calls are inside user-gesture handlers only.

2. Create `apps/studio/src/lib/io/fileState.svelte.ts`:
   - Module-level `$state` runes (per project pattern: history, clipboard, theme stores):
     - `let currentFileName = $state<string | null>(null)`
     - `let fileHandle = $state<FileSystemFileHandle | null>(null)`
     - `let isDirty = $state(false)`
   - Export getters: `getFileName()`, `getFileHandle()`, `getIsDirty()`
   - Export mutators: `markDirty()`, `markClean(name?, handle?)`, `resetFileState()`

3. Create `apps/studio/src/lib/io/export.ts`:
   - `exportAsCalm(json: string, filename?: string)`: Create Blob from json string, trigger download via `downloadDataUrl`. Default filename: `architecture.calm.json`.

4. Update `apps/studio/src/tests/fileSystem.test.ts` to fix imports and ensure tests pass GREEN.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio test -- src/tests/fileSystem.test.ts</automated>
  </verify>
  <done>All fileSystem tests pass. openFile/saveFile handle both File System Access API and fallback paths. fileState store tracks dirty state. exportAsCalm triggers CALM JSON download.</done>
</task>

<task type="auto">
  <name>Task 2: Create SVG, PNG, and calmscript export functions</name>
  <files>apps/studio/src/lib/io/export.ts</files>
  <action>
Extend `apps/studio/src/lib/io/export.ts` with SVG, PNG, and calmscript export functions:

1. `exportAsSvg(nodes: Node[])`:
   - Import `toSvg` from `html-to-image` and `getNodesBounds`, `getViewportForBounds` from `@xyflow/svelte`
   - Get bounds from nodes, compute viewport for 1920x1080 canvas area
   - Query `.svelte-flow__viewport` element — if null, log error and return (per RESEARCH Pitfall 3)
   - Call `toSvg(viewportEl, { backgroundColor: 'transparent', width: 1920, height: 1080, style: { transform, width, height } })`
   - Download via `downloadDataUrl(dataUrl, 'architecture.svg')`

2. `exportAsPng(nodes: Node[])`:
   - Same as SVG but use `toPng` with `pixelRatio: 2` (Retina, per user decision)
   - Download as `architecture.png`

3. `exportAsCalmscript(content: string)`:
   - Stub for Phase 4: accepts the current calmscript view content string
   - Downloads as `architecture.calmscript`
   - This will be fully functional after Phase 5 implements the DSL

Pin html-to-image to 1.11.11 (already installed in Plan 00). Per RESEARCH: newer versions have export regressions.

Import `type { Node }` from `@xyflow/svelte` for the nodes parameter type.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/studio test && pnpm --filter @calmstudio/studio build</automated>
  </verify>
  <done>export.ts exports all five functions: exportAsCalm, exportAsSvg, exportAsPng, exportAsCalmscript, downloadDataUrl. Build succeeds with no type errors.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @calmstudio/studio test` — all tests pass including fileSystem tests
2. `pnpm --filter @calmstudio/studio build` — builds without errors
3. Three modules exist: fileSystem.ts, fileState.svelte.ts, export.ts
</verification>

<success_criteria>
- File open works with File System Access API and input fallback
- File save works with handle reuse and Blob download fallback
- Dirty state tracked via $state store
- CALM JSON export triggers download
- SVG/PNG export functions ready (DOM capture requires browser to test)
- calmscript export stub ready
</success_criteria>

<output>
After completion, create `.planning/phases/04-import-export-layout/04-02-SUMMARY.md`
</output>
