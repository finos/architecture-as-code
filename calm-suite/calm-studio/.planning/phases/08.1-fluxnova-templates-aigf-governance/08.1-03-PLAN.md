---
phase: 08.1-fluxnova-templates-aigf-governance
plan: 03
type: execute
wave: 2
depends_on: ["08.1-01", "08.1-02"]
files_modified:
  - apps/studio/src/lib/templates/registry.ts
  - apps/studio/src/lib/templates/TemplatePicker.svelte
  - apps/studio/src/lib/templates/fluxnova-flash-risk.json
  - apps/studio/src/lib/templates/fluxnova-settlement.json
  - apps/studio/src/lib/templates/fluxnova-ai-agent.json
  - apps/studio/src/lib/templates/fluxnova-microservices.json
  - apps/studio/src/lib/toolbar/Toolbar.svelte
  - apps/studio/src/lib/io/export.ts
  - apps/studio/src/routes/+page.svelte
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "Template picker modal is accessible from toolbar Templates button"
    - "Template picker modal is accessible from empty canvas Start from template link"
    - "All 6 FluxNova templates load onto canvas with correct nodes, relationships, and controls"
    - "Category tabs filter templates (FluxNova, AI Governance, General)"
    - "Canvas dirty state prompts confirmation before template load"
    - "_template metadata is stripped from CALM JSON export"
  artifacts:
    - path: "apps/studio/src/lib/templates/registry.ts"
      provides: "Template registration, lookup, and category listing"
      exports: ["registerTemplate", "loadTemplate", "getTemplatesByCategory", "getAllCategories", "getAllTemplates", "initAllTemplates"]
    - path: "apps/studio/src/lib/templates/TemplatePicker.svelte"
      provides: "Full-screen modal with category tabs and template cards"
    - path: "apps/studio/src/lib/templates/fluxnova-flash-risk.json"
      provides: "Flash risk management FluxNova template"
    - path: "apps/studio/src/lib/templates/fluxnova-settlement.json"
      provides: "Post-trade settlement FluxNova template"
    - path: "apps/studio/src/lib/templates/fluxnova-ai-agent.json"
      provides: "FluxNova orchestrating AI agents template"
    - path: "apps/studio/src/lib/templates/fluxnova-microservices.json"
      provides: "FluxNova microservices via external tasks template"
  key_links:
    - from: "apps/studio/src/lib/templates/TemplatePicker.svelte"
      to: "apps/studio/src/lib/templates/registry.ts"
      via: "getTemplatesByCategory/getAllCategories calls"
      pattern: "getTemplatesByCategory|getAllCategories"
    - from: "apps/studio/src/routes/+page.svelte"
      to: "apps/studio/src/lib/templates/registry.ts"
      via: "initAllTemplates at module level + loadTemplate on selection"
      pattern: "initAllTemplates|loadTemplate"
    - from: "apps/studio/src/lib/io/export.ts"
      to: "template metadata stripping"
      via: "_template key deletion before export"
      pattern: "_template.*delete|_template.*strip"
---

<objective>
Create the template system: 4 new template JSON files, template registry, full-screen picker modal, toolbar integration, and _template stripping on export.

Purpose: Templates provide opinionated starting points for common architectures. The 6 FluxNova templates tell the enterprise financial services story for the OSFF Toronto demo — platform, KYC onboarding, flash risk, settlement, AI agent orchestration, and microservices.

Output: Working template picker accessible from toolbar and empty canvas, all 6 templates loadable, export strips template metadata.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-CONTEXT.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-RESEARCH.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-01-SUMMARY.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-02-SUMMARY.md

<interfaces>
<!-- Existing template format from fluxnova-platform.json -->
Template JSON structure:
```json
{
  "_template": {
    "id": "fluxnova-platform",
    "name": "FluxNova: Platform",
    "description": "Base FluxNova BPM platform deployment...",
    "category": "fluxnova",
    "tags": ["fluxnova", "bpm", ...],
    "version": "1.0.0",
    "author": "CalmStudio Contributors",
    "sourceRef": "finos/fluxnova-bpm-platform"
  },
  "nodes": [ ... CalmNode[] with controls ... ],
  "relationships": [ ... CalmRelationship[] with controls ... ]
}
```

<!-- Toolbar props pattern -->
From apps/studio/src/lib/toolbar/Toolbar.svelte:
```typescript
let {
  onopen, onsave, onsaveas, onnew, onvalidate,
  onexportcalm, onexportsvg, onexportpng, onexportcalmscript,
  onloaddemo, filename, isDirty, c4Level, onc4levelchange,
}: { ... } = $props();
```

<!-- Export function -->
From apps/studio/src/lib/io/export.ts:
```typescript
export function exportAsCalm(json: string, filename = 'architecture.calm.json'): void {
  // Blob download + sidecar generation
}
```

<!-- CalmArchitecture type from Plan 01 -->
```typescript
interface CalmArchitecture {
  nodes: CalmNode[];
  relationships: CalmRelationship[];
  decorators?: CalmDecorator[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template registry + 4 new template JSON files</name>
  <files>apps/studio/src/lib/templates/registry.ts, apps/studio/src/lib/templates/fluxnova-flash-risk.json, apps/studio/src/lib/templates/fluxnova-settlement.json, apps/studio/src/lib/templates/fluxnova-ai-agent.json, apps/studio/src/lib/templates/fluxnova-microservices.json</files>
  <action>
    1. Create `apps/studio/src/lib/templates/registry.ts`:
       - Define `CalmTemplate` interface: extends `CalmArchitecture` with `_template: { id: string; name: string; description: string; category: string; tags: string[]; version: string; author: string; sourceRef?: string; }`
       - `const templates = new Map<string, CalmTemplate>()`
       - `registerTemplate(t: CalmTemplate): void` — sets in map by `t._template.id`
       - `loadTemplate(id: string): CalmArchitecture` — finds by id, spreads out `_template`, returns plain `CalmArchitecture` (uses `const { _template, ...arch } = t; return arch;`)
       - `getTemplatesByCategory(category: string): CalmTemplate[]` — filters by category
       - `getAllCategories(): string[]` — returns unique sorted categories
       - `getAllTemplates(): CalmTemplate[]` — returns all templates
       - `initAllTemplates(): void` — imports all 6 template JSON files and registers them. Use static imports at module top (same pattern as initAllPacks). Import with `import fluxnovaPlatform from './fluxnova-platform.json';` etc. SvelteKit/Vite handles JSON imports natively in the app package (unlike calm-core).
       - Export all functions and the CalmTemplate type

    2. Create 4 new template JSON files following the exact format of existing `fluxnova-platform.json`:

       **fluxnova-flash-risk.json** — Flash risk management blueprint:
       - _template: id="fluxnova-flash-risk", name="FluxNova: Flash Risk Management", category="fluxnova"
       - Nodes: All 7 base platform nodes (fluxnova-platform, engine, rest-api, cockpit, admin, tasklist, process-db) PLUS:
         - risk-compute-onprem (service) — "On-premise risk computation engine for latency-sensitive calculations"
         - risk-compute-cloud (service) — "Cloud-based risk computation for burst capacity"
         - risk-aggregation-svc (service) — "Aggregates risk results from on-prem and cloud compute nodes"
         - cloud-provisioner (service) — "Auto-provisions cloud compute instances based on market volatility"
       - Relationships: All 11 base platform rels PLUS appropriate connects between engine->new services, aggregation connects
       - Controls: data-classification "Confidential" on compute nodes

       **fluxnova-settlement.json** — Post-trade settlement blueprint:
       - _template: id="fluxnova-settlement", name="FluxNova: Post-Trade Settlement", category="fluxnova"
       - Nodes: All 7 base platform nodes PLUS:
         - counterparty-gateway (service) — "Secure gateway for counterparty trade confirmations and matching"
         - clearing-house-connector (service) — "Connector to central clearing house for trade novation and netting"
         - regulatory-reporting-svc (service) — "Automated regulatory reporting (EMIR, MiFIR, Dodd-Frank)"
         - settlement-db (database) — "Settlement positions, obligations, and trade lifecycle data"
       - Relationships: All 11 base rels PLUS engine orchestrates settlement flow
       - Controls: audit-logging, encryption-in-transit, regulatory-compliance on external connections

       **fluxnova-ai-agent.json** — FluxNova orchestrating AI agents:
       - _template: id="fluxnova-ai-agent", name="FluxNova: AI Agent Orchestration", category="fluxnova"
       - Nodes: All 7 base platform nodes PLUS:
         - ai-agent (ai:agent) — "Autonomous AI agent executing tasks from FluxNova process"
         - ai-llm (ai:llm) — "Large language model providing inference for agent decisions"
         - ai-guardrail (ai:guardrail) — "Safety filter validating agent inputs and outputs"
         - ai-tool (ai:tool) — "Callable function exposed to the AI agent"
       - Relationships: All 11 base rels PLUS engine->ai-agent (connects, HTTPS), agent->llm (connects), guardrail->agent (connects), agent->tool (connects)
       - Controls: AIGF controls on AI nodes (aigf-least-privilege on agent, aigf-version-pinning on llm)

       **fluxnova-microservices.json** — FluxNova orchestrating microservices:
       - _template: id="fluxnova-microservices", name="FluxNova: Microservices Orchestration", category="fluxnova"
       - Nodes: All 7 base platform nodes PLUS:
         - payment-worker (fluxnova:external-task-worker) — "Processes payment transaction tasks"
         - notification-worker (fluxnova:external-task-worker) — "Handles notification delivery tasks"
         - fraud-check-worker (fluxnova:external-task-worker) — "Executes fraud detection tasks"
         - message-broker (service) — "Async event bus for worker communication"
         - api-gateway (service) — "Entry point gateway for external API consumers"
       - Relationships: All 11 base rels PLUS workers->rest-api (connects, HTTPS "polls for external tasks"), broker connects

       Each template MUST:
       - Have unique-ids for ALL nodes and relationships (no collisions across templates)
       - Use proper CALM relationship-types (connects, composed-of, deployed-in)
       - Include protocol on connects relationships
       - Have descriptions on all nodes and relationships
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && node -e "
      const fs = require('fs');
      const files = ['fluxnova-platform','fluxnova-kyc-onboarding','fluxnova-flash-risk','fluxnova-settlement','fluxnova-ai-agent','fluxnova-microservices'];
      let ok = true;
      for (const f of files) {
        try {
          const d = JSON.parse(fs.readFileSync('apps/studio/src/lib/templates/' + f + '.json', 'utf-8'));
          if (!d._template?.id) { console.error(f + ': missing _template.id'); ok=false; }
          if (!d.nodes?.length) { console.error(f + ': no nodes'); ok=false; }
          if (!d.relationships?.length) { console.error(f + ': no relationships'); ok=false; }
          console.log(f + ': ' + d.nodes.length + ' nodes, ' + d.relationships.length + ' rels');
        } catch(e) { console.error(f + ': ' + e.message); ok=false; }
      }
      if (!ok) process.exit(1);
      console.log('All 6 templates valid');
    "</automated>
  </verify>
  <done>Template registry created with registerTemplate, loadTemplate, getTemplatesByCategory, initAllTemplates. All 6 template JSON files exist with valid structure.</done>
</task>

<task type="auto">
  <name>Task 2: Template picker modal + toolbar button + export stripping + page wiring</name>
  <files>apps/studio/src/lib/templates/TemplatePicker.svelte, apps/studio/src/lib/toolbar/Toolbar.svelte, apps/studio/src/lib/io/export.ts, apps/studio/src/routes/+page.svelte</files>
  <action>
    1. Create `apps/studio/src/lib/templates/TemplatePicker.svelte`:
       - Full-screen modal overlay (fixed position, z-50, backdrop blur) for maximum visual impact at OSFF demo (per locked decision)
       - Category tabs at top: 'FluxNova', 'AI Governance', 'General' (per locked decision). Show all categories from `getAllCategories()` but ensure those 3 are primary.
       - Tab names: capitalize first letter of category, map 'fluxnova' -> 'FluxNova', 'ai-governance' -> 'AI Governance', 'general' -> 'General'
       - Compact cards with: category icon area (small colored dot matching pack color or neutral), template name (bold), one-line description, tag pills, node count badge
       - Cards laid out in a responsive grid (2-3 columns on desktop, 1 on mobile)
       - Clicking a card fires `onselect(templateId: string)` callback
       - Cancel button and Escape key fire `oncancel()` callback
       - Close on backdrop click (outside the modal content area)
       - Props: `onselect: (id: string) => void`, `oncancel: () => void`
       - Import `getTemplatesByCategory`, `getAllCategories`, `getAllTemplates` from `./registry`
       - SPDX header

    2. Update `apps/studio/src/lib/toolbar/Toolbar.svelte`:
       - Add `ontemplates` prop: `ontemplates?: () => void`
       - Add a "Templates" button in the toolbar-left section (after the CalmStudio app name, before the C4 selector)
       - Style: same button styling as existing toolbar buttons (compact, icon + text)
       - Use a grid/template icon (simple SVG inline) + "Templates" label

    3. Update `apps/studio/src/lib/io/export.ts`:
       - In `exportAsCalm()`, before creating the Blob, parse the JSON, strip `_template` key if present, then re-stringify:
         ```
         const parsed = JSON.parse(json);
         if ('_template' in parsed) { delete parsed._template; }
         const cleanJson = JSON.stringify(parsed, null, 2);
         ```
       - Use `cleanJson` for the Blob instead of raw `json`
       - The sidecar detection should also use the clean parsed object

    4. Update `apps/studio/src/routes/+page.svelte`:
       - Import `initAllTemplates`, `loadTemplate` from `$lib/templates/registry`
       - Import `TemplatePicker` from `$lib/templates/TemplatePicker.svelte`
       - Call `initAllTemplates()` at module level (alongside existing `initAllPacks()`)
       - Add `let showTemplatePicker = $state(false)` state
       - Add `handleTemplateLoad(templateId: string)` function:
         - If canvas is dirty, prompt confirmation (use `window.confirm('You have unsaved changes. Load template anyway?')`)
         - Call `loadTemplate(templateId)` to get clean CalmArchitecture
         - Apply to canvas using the same import path as file import (convert to JSON string, pass to existing `applyFromJson` or equivalent)
         - Set `showTemplatePicker = false`
       - Wire Toolbar `ontemplates={() => showTemplatePicker = true}`
       - Add empty canvas "Start from template" link — when canvas has 0 nodes, show a centered link/button. This should appear where the canvas is empty (check existing empty state pattern or add one).
       - Render `{#if showTemplatePicker}<TemplatePicker onselect={handleTemplateLoad} oncancel={() => showTemplatePicker = false} />{/if}`
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter studio build</automated>
  </verify>
  <done>Template picker accessible from toolbar and empty canvas. Templates load onto canvas. Export strips _template metadata. Dirty state confirmation works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of template system</name>
  <files>apps/studio/src/lib/templates/TemplatePicker.svelte</files>
  <action>
    Human verifies the template system works end-to-end:
    1. Run `pnpm --filter studio dev` and open http://localhost:5173
    2. On empty canvas, look for "Start from template" link — click it. Template picker modal should open.
    3. Verify category tabs appear: FluxNova should have 6 templates
    4. Click "FluxNova: Platform" card — canvas should populate with 7 nodes + 11 relationships
    5. Click Templates button in toolbar — picker should reopen (confirm dirty state dialog appears first)
    6. Load "FluxNova: AI Agent Orchestration" template — should show FluxNova nodes + AI nodes
    7. Export as CALM JSON (File > Export > CALM JSON) — open exported file and confirm NO "_template" key exists
    8. Verify all 6 templates are present and loadable
    Resume signal: Type "approved" or describe issues
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter studio build</automated>
  </verify>
  <done>Human confirmed template picker, template loading, category tabs, dirty state confirmation, and export stripping all work correctly.</done>
</task>

</tasks>

<verification>
- All 6 template JSON files parse correctly
- Template picker renders as full-screen modal with category tabs
- Templates load onto canvas and nodes render with correct pack icons/colors
- Export produces valid CALM JSON without _template metadata
- `pnpm --filter studio build` succeeds
</verification>

<success_criteria>
- Template picker accessible from toolbar "Templates" button and empty canvas link
- All 6 FluxNova templates load correctly
- Category tabs filter templates
- Dirty state confirmation before template load
- _template stripped from CALM JSON export
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-03-SUMMARY.md`
</output>
