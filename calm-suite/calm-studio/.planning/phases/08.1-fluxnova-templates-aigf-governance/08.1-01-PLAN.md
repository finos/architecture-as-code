---
phase: 08.1-fluxnova-templates-aigf-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/calm-core/src/types.ts
  - packages/calm-core/src/index.ts
  - packages/calm-core/src/aigf/types.ts
  - packages/calm-core/src/aigf/catalogue.ts
  - packages/calm-core/src/aigf/mappings.ts
  - packages/calm-core/vitest.config.ts
  - packages/calm-core/src/aigf/catalogue.test.ts
  - packages/calm-core/src/aigf/mappings.test.ts
autonomous: true
requirements:
  - CTRL-01
  - CTRL-03
  - AIGF-01
  - AIGF-04

must_haves:
  truths:
    - "CalmNode and CalmRelationship accept controls, data-classification, and metadata fields without TypeScript errors"
    - "CalmArchitecture accepts an optional decorators array"
    - "CalmDecorator and CalmEvidence types exist for CALM 1.2 roundtrip"
    - "AIGF catalogue contains exactly 23 risks and 23 mitigations importable as typed arrays"
    - "getAIGFForNodeType('ai:llm') returns applicable risks and recommended mitigations"
    - "AI node types without governance mappings (ai:guardrail, ai:human-in-the-loop, ai:eval-monitor) return empty risk arrays"
  artifacts:
    - path: "packages/calm-core/src/types.ts"
      provides: "CalmControls, CalmControl, CalmControlRequirement, CalmDecorator, CalmEvidence types; extended CalmNode and CalmRelationship"
      exports: ["CalmControls", "CalmControl", "CalmControlRequirement", "CalmDecorator", "CalmEvidence"]
    - path: "packages/calm-core/src/aigf/types.ts"
      provides: "AIGFRisk, AIGFMitigation, AIGFExternalRefs, AIGFNodeRiskMapping interfaces"
      exports: ["AIGFRisk", "AIGFMitigation", "AIGFRiskType", "AIGFMitigationType", "AIGFExternalRefs", "AIGFNodeRiskMapping"]
    - path: "packages/calm-core/src/aigf/catalogue.ts"
      provides: "Static AIGF v2.0 risk and mitigation data"
      exports: ["aigfRisks", "aigfMitigations"]
    - path: "packages/calm-core/src/aigf/mappings.ts"
      provides: "Node-type-to-risk mappings and lookup function"
      exports: ["aigfNodeRiskMappings", "getAIGFForNodeType", "isAINode"]
  key_links:
    - from: "packages/calm-core/src/aigf/catalogue.ts"
      to: "packages/calm-core/src/aigf/types.ts"
      via: "typed exports matching AIGFRisk/AIGFMitigation"
      pattern: "import.*AIGFRisk.*from.*types"
    - from: "packages/calm-core/src/aigf/mappings.ts"
      to: "packages/calm-core/src/aigf/catalogue.ts"
      via: "risk/mitigation lookup by ID"
      pattern: "aigfRisks\\.find|aigfMitigations\\.filter"
---

<objective>
Add CALM 1.2 controls/decorator/evidence type support to calm-core and create the AIGF data package with risk-to-node mappings.

Purpose: This is the type foundation for the entire phase. Every other plan (templates, controls UI, governance panel, validation rules) depends on these types existing. Controls enable CALM 1.2 compliance; AIGF data enables design-time governance.

Output: Extended calm-core types, AIGF data package (`aigf/` directory), vitest config for calm-core, passing unit tests.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-CONTEXT.md
@.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/calm-core/src/types.ts (CURRENT — to be extended):
```typescript
export interface CalmNode {
  'unique-id': string;
  'node-type': CalmNodeType | string;
  name: string;
  description?: string;
  interfaces?: CalmInterface[];
  customMetadata?: Record<string, string>;
}

export interface CalmRelationship {
  'unique-id': string;
  'relationship-type': CalmRelationshipType;
  source: string;
  destination: string;
  protocol?: string;
  description?: string;
}

export interface CalmArchitecture {
  nodes: CalmNode[];
  relationships: CalmRelationship[];
}
```

From packages/calm-core/src/index.ts:
```typescript
export * from './types.js';
export * from './validation.js';
```

From docs/AIGF_CATALOGUE.json (shape):
```json
{
  "version": "2.0",
  "risks": [{ "id": "AIR-RC-001", "sequence": 1, "title": "...", "type": "RC", "description": "...", "externalRefs": {...}, "relatedRisks": [...] }],
  "mitigations": [{ "id": "mi-20", "sequence": 20, "title": "...", "type": "PREV", "description": "...", "externalRefs": {...}, "mitigates": [...], "relatedMitigations": [...], "calmControlKey": "aigf-mcp-security" }]
}
```

From packages/calm-core/tsconfig.json:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src"]
}
```

Note: tsconfig.base.json does NOT have `resolveJsonModule: true`. AIGF data must be inlined as typed TypeScript constants, not JSON imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CALM 1.2 types + vitest config + AIGF type definitions</name>
  <files>packages/calm-core/src/types.ts, packages/calm-core/src/aigf/types.ts, packages/calm-core/vitest.config.ts, packages/calm-core/src/index.ts</files>
  <behavior>
    - Test: CalmNode with controls property compiles without error
    - Test: CalmNode with data-classification property compiles without error
    - Test: CalmRelationship with controls and metadata properties compiles without error
    - Test: CalmArchitecture with decorators array compiles without error
    - Test: CalmDecorator has required fields (unique-id, type, target, applies-to, data)
    - Test: CalmEvidence has required fields (unique-id, evidence-paths, control-config-url)
  </behavior>
  <action>
    1. Create `packages/calm-core/vitest.config.ts` following the exact pattern from `packages/extensions/vitest.config.ts` (defineConfig with test.include: ['src/**/*.test.ts']).

    2. Extend `packages/calm-core/src/types.ts`:
       - Add `CalmControlRequirement` interface: `'requirement-url': string`, `'config-url'?: string`, `config?: Record<string, unknown>` (all optional except requirement-url, per Pitfall 1 in RESEARCH — do NOT enforce oneOf strictly)
       - Add `CalmControl` interface: `description: string`, `requirements: CalmControlRequirement[]`
       - Add `CalmControls` type: `Record<string, CalmControl>`
       - Add to `CalmNode`: `controls?: CalmControls`, `'data-classification'?: string`, `metadata?: Record<string, unknown>`
       - Add to `CalmRelationship`: `controls?: CalmControls`, `metadata?: Record<string, unknown>`
       - Add `CalmDecorator` interface: `'unique-id': string`, `type: string`, `target: string[]`, `'applies-to': string[]`, `data: Record<string, unknown>`
       - Add `CalmEvidence` interface: `'unique-id': string`, `'evidence-paths': string[]`, `'control-config-url': string`
       - Add to `CalmArchitecture`: `decorators?: CalmDecorator[]`
       - Export all new types

    3. Create `packages/calm-core/src/aigf/types.ts`:
       - `AIGFRiskType = 'OP' | 'SEC' | 'RC'`
       - `AIGFMitigationType = 'PREV' | 'DET'`
       - `AIGFExternalRefs` interface with all 8 framework fields (owaspLlm, owaspMl, nistAi600, ffiec, euAiAct, nistSp80053r5, iso42001, mitreAtlas) — all `string[]` optional
       - `AIGFRisk` interface: id, sequence, title, type, description, externalRefs, relatedRisks
       - `AIGFMitigation` interface: id, sequence, title, type, description, externalRefs, mitigates, relatedMitigations, calmControlKey
       - `AIGFNodeRiskMapping` interface: nodeTypePattern, applicableRisks, recommendedMitigations
       - Export all types

    4. Update `packages/calm-core/src/index.ts` to add: `export * from './aigf/types.js';`
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/calm-core typecheck</automated>
  </verify>
  <done>CalmNode/CalmRelationship accept controls, data-classification, metadata. CalmDecorator/CalmEvidence types exist. AIGFRisk/AIGFMitigation types defined. All types export cleanly from calm-core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AIGF catalogue data + node-to-risk mappings + unit tests</name>
  <files>packages/calm-core/src/aigf/catalogue.ts, packages/calm-core/src/aigf/mappings.ts, packages/calm-core/src/aigf/catalogue.test.ts, packages/calm-core/src/aigf/mappings.test.ts, packages/calm-core/src/index.ts</files>
  <behavior>
    - Test: aigfRisks has exactly 23 entries
    - Test: aigfMitigations has exactly 23 entries
    - Test: Every risk has id, title, type, description, externalRefs
    - Test: Every mitigation has id, title, type, calmControlKey
    - Test: Risk types are only 'OP', 'SEC', or 'RC'
    - Test: getAIGFForNodeType('ai:llm') returns non-empty risks and mitigations
    - Test: getAIGFForNodeType('ai:agent') returns agent-specific risks (AIR-SEC-024, AIR-OP-018)
    - Test: getAIGFForNodeType('ai:guardrail') returns empty risks (guardrails ARE the mitigation)
    - Test: getAIGFForNodeType('ai:human-in-the-loop') returns empty risks
    - Test: getAIGFForNodeType('ai:eval-monitor') returns empty risks
    - Test: getAIGFForNodeType('service') returns empty (non-AI node)
    - Test: isAINode returns true for ai:llm, false for service, false for fluxnova:engine
  </behavior>
  <action>
    1. Create `packages/calm-core/src/aigf/catalogue.ts`:
       - Inline all 23 risks from `docs/AIGF_CATALOGUE.json` as `export const aigfRisks: AIGFRisk[]` (do NOT use JSON import — tsconfig lacks resolveJsonModule).
       - Inline all 23 mitigations as `export const aigfMitigations: AIGFMitigation[]`.
       - Copy data exactly from the JSON file. Maintain the id, sequence, title, type, description, externalRefs, relatedRisks fields for risks. Include calmControlKey on mitigations.
       - The file will be ~400-500 lines of static data. This is expected.

    2. Create `packages/calm-core/src/aigf/mappings.ts`:
       - Import `AIGFRisk`, `AIGFMitigation`, `AIGFNodeRiskMapping` from `./types.js`
       - Import `aigfRisks`, `aigfMitigations` from `./catalogue.js`
       - Define `aigfNodeRiskMappings: AIGFNodeRiskMapping[]` with all entries from the PRD B1.3 table (see RESEARCH Pattern 4):
         - ai:llm -> AIR-OP-004, AIR-OP-005, AIR-OP-006, AIR-RC-001 / mi-10, mi-3, mi-1, mi-15
         - ai:agent -> AIR-SEC-024, AIR-OP-018, AIR-OP-028 / mi-18, mi-21, mi-22
         - ai:orchestrator -> AIR-OP-028, AIR-SEC-025 / mi-22, mi-19, mi-21
         - ai:vector-store -> AIR-SEC-002, AIR-SEC-009 / mi-2, mi-12, mi-14, mi-6
         - ai:tool -> AIR-SEC-025 / mi-19
         - ai:memory -> AIR-SEC-027 / mi-23, mi-14
         - ai:guardrail -> (empty risks, empty mitigations)
         - ai:rag-pipeline -> AIR-OP-004, AIR-SEC-002 / mi-13, mi-2, mi-6
         - ai:knowledge-base -> AIR-SEC-009, AIR-OP-019 / mi-6, mi-16
         - ai:embedding-model -> AIR-SEC-008, AIR-OP-005 / mi-10, mi-5
         - ai:api-gateway -> AIR-SEC-010, AIR-OP-007 / mi-3, mi-17, mi-8
         - ai:human-in-the-loop -> (empty)
         - ai:eval-monitor -> (empty)
       - Export `function isAINode(nodeOrType: { 'node-type': string } | string): boolean` — returns true if node-type starts with 'ai:'
       - Export `function getAIGFForNodeType(nodeType: string): { risks: AIGFRisk[]; mitigations: AIGFMitigation[] }` — finds matching mapping entry, resolves risk IDs to full risk objects and mitigation IDs to full mitigation objects

    3. Update `packages/calm-core/src/index.ts`:
       - Add: `export * from './aigf/catalogue.js';`
       - Add: `export * from './aigf/mappings.js';`

    4. Create test files `catalogue.test.ts` and `mappings.test.ts` with the behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/calm-core test</automated>
  </verify>
  <done>AIGF catalogue exports 23 risks + 23 mitigations. getAIGFForNodeType resolves risks and mitigations for all 13 AI node types. isAINode correctly identifies AI nodes. All unit tests green.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @calmstudio/calm-core typecheck` passes (types compile)
- `pnpm --filter @calmstudio/calm-core test` passes (all unit tests green)
- `pnpm -r test` passes (no regressions in other packages)
</verification>

<success_criteria>
- CalmNode and CalmRelationship TypeScript types include controls, data-classification, metadata
- CalmDecorator and CalmEvidence types exist in calm-core
- AIGF data package exports 23 risks and 23 mitigations as typed arrays
- Node-to-risk mapping resolves correctly for all 13 AI pack node types
- calm-core typecheck and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-fluxnova-templates-aigf-governance/08.1-01-SUMMARY.md`
</output>
