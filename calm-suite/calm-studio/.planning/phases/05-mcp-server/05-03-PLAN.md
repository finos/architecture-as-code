---
phase: 05-mcp-server
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - MCPS-07

must_haves:
  truths:
    - "MCP Inspector connects to the server and lists all registered tools"
    - "A tool call through MCP Inspector returns a valid content response"
    - "Server works end-to-end: create architecture -> add node -> validate -> render SVG"
  artifacts: []
  key_links:
    - from: "MCP Inspector"
      to: "packages/mcp-server/dist/index.js"
      via: "stdio transport"
      pattern: "npx @modelcontextprotocol/inspector"
---

<objective>
Verify the MCP server works end-to-end with MCP Inspector and through a real tool call workflow.

Purpose: Prove MCP protocol compliance and that the server works as a real AI assistant would use it.
Output: Confirmed working MCP server ready for Claude Code registration.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-mcp-server/05-CONTEXT.md
@.planning/phases/05-mcp-server/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration test via CLI</name>
  <files></files>
  <action>
Run the MCP server through a real workflow without MCP Inspector (direct file verification):

1. Build the server: `pnpm --filter @calmstudio/mcp build`
2. Create a test directory: `mktemp -d`
3. Test the server by writing a small Node.js script that:
   - Imports `createServer` from the built dist
   - Calls tool handlers directly (bypassing transport) to verify:
     a. `read_calm_guide` returns CALM reference text
     b. `create_architecture` with 3 nodes (actor, system, database) + 2 relationships writes a valid .calm file
     c. `add_node` adds a 4th node
     d. `add_relationship` adds a relationship to the new node
     e. `add_relationship` with dangling ref returns isError: true
     f. `validate_architecture` returns no errors for the valid file
     g. `describe_architecture` returns summary with correct counts
     h. `render_diagram` returns string containing `<svg`
     i. `export_calm` copies to a new path, `import_calm` reads it back
   - Verify each response has the correct `content[0].type === 'text'` and `isError` field
4. Alternatively, if direct import is complex, use the file system: run each tool's logic function from tests/ and verify the .calm file on disk.
5. Clean up temp directory.

This serves as the smoke test that MCPS-07 "works with any MCP-compatible AI assistant" is achievable — the protocol compliance is handled by the SDK, this verifies our tool handlers produce correct responses.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/mcp test && pnpm --filter @calmstudio/mcp build && node packages/mcp-server/dist/index.js --version</automated>
  </verify>
  <done>All tool handlers produce correct MCP response format, end-to-end workflow creates valid .calm file, server binary runs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MCP Inspector compliance verification</name>
  <action>
Human verifies MCP server compliance via MCP Inspector.
What was built: Complete MCP server with 15+ tools for CALM architecture creation, modification, validation, and rendering. Supports stdio (Claude Code) and HTTP (Claude Cowork) transports.
  </action>
  <verify>
    1. Build: `cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/mcp build`
    2. Run MCP Inspector: `npx @modelcontextprotocol/inspector packages/mcp-server/dist/index.js`
    3. In the MCP Inspector UI:
       a. Verify "tools/list" shows all registered tools (create_architecture, add_node, add_relationship, validate_architecture, render_diagram, read_calm_guide, export_calm, import_calm, describe_architecture, get_node, update_node, delete_node, query_nodes, batch_create_nodes, get_relationship, update_relationship, delete_relationship, create_view, update_view)
       b. Call `read_calm_guide` — should return CALM type reference text
       c. Call `create_architecture` with: `{"nodes": [{"unique-id": "actor-1", "node-type": "actor", "name": "User"}, {"unique-id": "svc-1", "node-type": "service", "name": "API Gateway"}], "relationships": [{"unique-id": "rel-1", "relationship-type": "connects", "source": "actor-1", "destination": "svc-1", "protocol": "HTTPS"}]}`
       d. Call `validate_architecture` — should report no errors
       e. Call `render_diagram` — should return SVG string
    4. Optional: Register in Claude Code: `claude mcp add --transport stdio calmstudio -- node /Users/gshah/work/apps/excalicalm/calmstudio/packages/mcp-server/dist/index.js`
    5. Ask Claude Code to "create a simple microservices architecture with an API gateway, user service, and database"
  </verify>
  <done>MCP Inspector lists all tools, tool calls return valid responses, server is Claude Code compatible</done>
</task>

</tasks>

<verification>
- MCP Inspector lists all tools
- Tool calls return valid MCP content responses
- End-to-end workflow: create -> add -> validate -> render produces correct results
- Server binary starts without errors
</verification>

<success_criteria>
MCP server passes MCP Inspector compliance, all tools respond correctly, and optionally works with Claude Code to create an architecture from a text description.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-server/05-03-SUMMARY.md`
</output>
