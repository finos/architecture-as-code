---
phase: 05-mcp-server
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/mcp-server/package.json
  - packages/mcp-server/tsconfig.json
  - packages/mcp-server/vitest.config.ts
  - packages/mcp-server/src/types.ts
  - packages/mcp-server/src/file-io.ts
  - packages/mcp-server/src/validation.ts
  - packages/mcp-server/src/tests/createArchitecture.test.ts
  - packages/mcp-server/src/tests/nodes.test.ts
  - packages/mcp-server/src/tests/relationships.test.ts
  - packages/mcp-server/src/tests/validate.test.ts
  - packages/mcp-server/src/tests/render.test.ts
  - packages/mcp-server/src/tests/io.test.ts
autonomous: true
requirements:
  - MCPS-01
  - MCPS-02
  - MCPS-03
  - MCPS-04
  - MCPS-05
  - MCPS-06

must_haves:
  truths:
    - "pnpm --filter @calmstudio/mcp test runs and all stubs pass (trivially)"
    - "pnpm --filter @calmstudio/mcp build compiles TypeScript to dist/"
    - "Zod schemas define input contracts for all tool handlers"
    - "File I/O layer provides read/write/auto-init for .calm files"
  artifacts:
    - path: "packages/mcp-server/package.json"
      provides: "Published package config with bin, deps, build/test scripts"
      contains: "@calmstudio/mcp"
    - path: "packages/mcp-server/tsconfig.json"
      provides: "Node16 moduleResolution override for CLI compatibility"
      contains: "Node16"
    - path: "packages/mcp-server/vitest.config.ts"
      provides: "Test configuration for mcp-server package"
      contains: "vitest"
    - path: "packages/mcp-server/src/types.ts"
      provides: "Zod schemas for all tool inputs + response helpers"
      exports: ["NodeInputSchema", "RelationshipInputSchema", "CreateArchitectureSchema", "toolSuccess", "toolError"]
    - path: "packages/mcp-server/src/file-io.ts"
      provides: "File I/O layer for .calm and .calmstudio.json files"
      exports: ["readCalmFile", "writeCalmFile", "resolveFile"]
    - path: "packages/mcp-server/src/validation.ts"
      provides: "CALM schema validation pure function"
      exports: ["validateArchitecture"]
  key_links:
    - from: "packages/mcp-server/src/types.ts"
      to: "@calmstudio/calm-core"
      via: "import CalmNode, CalmRelationship, CalmArchitecture types"
      pattern: "import.*@calmstudio/calm-core"
    - from: "packages/mcp-server/src/file-io.ts"
      to: "node:fs"
      via: "readFileSync/writeFileSync for .calm files"
      pattern: "import.*node:fs"
---

<objective>
Set up the MCP server package with dependencies, build pipeline, Zod input schemas, file I/O layer, validation layer, and test stubs for all tool handlers.

Purpose: Establish the foundation that all tool handler implementations build on — types, file I/O, validation, and test infrastructure.
Output: Compilable package with test stubs, Zod schemas, file-io.ts, validation.ts, vitest config.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-server/05-CONTEXT.md
@.planning/phases/05-mcp-server/05-RESEARCH.md
@.planning/phases/05-mcp-server/05-VALIDATION.md

<interfaces>
<!-- Key types from calm-core that the MCP server will import -->

From packages/calm-core/src/types.ts:
```typescript
export type CalmNodeType = 'actor' | 'system' | 'service' | 'database' | 'network' | 'webclient' | 'ecosystem' | 'ldap' | 'data-asset';
export type CalmRelationshipType = 'connects' | 'interacts' | 'deployed-in' | 'composed-of' | 'options';
export interface CalmInterface { 'unique-id': string; type: string; value?: string; }
export interface CalmNode { 'unique-id': string; 'node-type': CalmNodeType | string; name: string; description?: string; interfaces?: CalmInterface[]; customMetadata?: Record<string, string>; }
export interface CalmRelationship { 'unique-id': string; 'relationship-type': CalmRelationshipType; source: string; destination: string; protocol?: string; description?: string; }
export interface CalmArchitecture { nodes: CalmNode[]; relationships: CalmRelationship[]; }
```

From packages/mcp-server/package.json (current stub):
```json
{ "name": "@calmstudio/mcp-server", "version": "0.0.0", "private": true, "type": "module" }
```

From tsconfig.base.json:
```json
{ "compilerOptions": { "moduleResolution": "bundler" } }
```
Note: mcp-server MUST override to "Node16" — see RESEARCH Pitfall 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package config, deps, build pipeline, vitest, Zod schemas</name>
  <files>packages/mcp-server/package.json, packages/mcp-server/tsconfig.json, packages/mcp-server/vitest.config.ts, packages/mcp-server/src/types.ts</files>
  <action>
1. Update `packages/mcp-server/package.json`:
   - Change `name` to `"@calmstudio/mcp"` (per CONTEXT.md — public package name, not the stub name)
   - Set `"private": false`
   - Add `"bin": { "calmstudio-mcp": "./dist/index.js" }`
   - Add `"files": ["dist"]`
   - Add dependencies: `@modelcontextprotocol/sdk` ^1.27.1, `zod` ^3.24.0, `elkjs` ^0.11.1, `elkjs-svg` latest
   - Add devDependencies: `vitest` ^3.0.8, `typescript` ^5.7.0, `@calmstudio/calm-core` `workspace:*`
   - Update scripts:
     - `"build": "tsc && chmod 755 dist/index.js"`
     - `"test": "vitest run"`
     - `"typecheck": "tsc --noEmit"`
   - Add `@calmstudio/calm-core` as a dependency: `"@calmstudio/calm-core": "workspace:*"`

2. Update `packages/mcp-server/tsconfig.json`:
   - Override `module` to `"Node16"` and `moduleResolution` to `"Node16"` (per RESEARCH Pitfall 6 — bundler resolution breaks standalone Node.js CLI)
   - Keep `outDir: "dist"`, `rootDir: "src"`
   - Add `"declaration": true`

3. Create `packages/mcp-server/vitest.config.ts`:
   - Simple config: `import { defineConfig } from 'vitest/config'; export default defineConfig({ test: { include: ['src/tests/**/*.test.ts'] } });`

4. Create `packages/mcp-server/src/types.ts` with Zod schemas for ALL tool inputs:
   - `NodeInputSchema`: z.object with unique-id (string), node-type (string), name (string), description (optional string), interfaces (optional array of InterfaceInputSchema), customMetadata (optional record)
   - `InterfaceInputSchema`: z.object with unique-id (string), type (string), value (optional string)
   - `RelationshipInputSchema`: z.object with unique-id (string), relationship-type (z.enum of 5 types), source (string), destination (string), protocol (optional string), description (optional string)
   - `CreateArchitectureSchema`: z.object with nodes (array of NodeInputSchema), relationships (optional array of RelationshipInputSchema, default []), file (optional string, describe default path)
   - `AddNodeSchema`: z.object with node (NodeInputSchema), file (optional string)
   - `AddRelationshipSchema`: z.object with relationship (RelationshipInputSchema), file (optional string)
   - `ValidateArchitectureSchema`: z.object with file (optional string)
   - `RenderDiagramSchema`: z.object with file (optional string), direction (optional z.enum DOWN/RIGHT/UP, default DOWN)
   - `ExportCalmSchema`: z.object with source (optional string), destination (string)
   - `ImportCalmSchema`: z.object with file (string)
   - `DescribeArchitectureSchema`: z.object with file (optional string)
   - `GetNodeSchema`: z.object with id (string), file (optional string)
   - `UpdateNodeSchema`: z.object with id (string), updates (partial NodeInputSchema without unique-id), file (optional string)
   - `DeleteNodeSchema`: z.object with id (string), file (optional string)
   - `QueryNodesSchema`: z.object with type (optional string), name (optional string), file (optional string)
   - `BatchCreateNodesSchema`: z.object with nodes (array of NodeInputSchema), file (optional string)
   - `GetRelationshipSchema`, `UpdateRelationshipSchema`, `DeleteRelationshipSchema`: similar patterns
   - `CreateViewSchema`: z.object with file (optional string)
   - `UpdateViewSchema`: z.object with file (optional string)
   - Helper functions: `toolSuccess(text: string)` returns `{ content: [{ type: 'text', text }], isError: false }`, `toolError(text: string)` returns `{ content: [{ type: 'text', text }], isError: true }`
   - Add SPDX header to file

5. Run `pnpm install` from workspace root to install new deps.

IMPORTANT: All `.ts` files in mcp-server that import local modules MUST use `.js` extensions in import paths (e.g., `import { readCalmFile } from './file-io.js'`) because Node16 moduleResolution requires explicit extensions.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/mcp build && pnpm --filter @calmstudio/mcp typecheck</automated>
  </verify>
  <done>Package compiles with tsc, all Zod schemas exported from types.ts, vitest config exists, deps installed</done>
</task>

<task type="auto">
  <name>Task 2: File I/O layer, validation layer, and test stubs</name>
  <files>packages/mcp-server/src/file-io.ts, packages/mcp-server/src/validation.ts, packages/mcp-server/src/tests/createArchitecture.test.ts, packages/mcp-server/src/tests/nodes.test.ts, packages/mcp-server/src/tests/relationships.test.ts, packages/mcp-server/src/tests/validate.test.ts, packages/mcp-server/src/tests/render.test.ts, packages/mcp-server/src/tests/io.test.ts</files>
  <action>
1. Create `packages/mcp-server/src/file-io.ts`:
   - `resolveFile(file?: string): string` — returns `path.resolve(file ?? './architecture.calm')`
   - `readCalmFile(filePath: string): CalmArchitecture` — reads file, JSON.parse, returns architecture. If file doesn't exist, returns `{ nodes: [], relationships: [] }` (auto-init per RESEARCH Pitfall 8 — NOT an error). Validate parsed JSON has `nodes` array (at minimum).
   - `writeCalmFile(filePath: string, arch: CalmArchitecture): void` — JSON.stringify(arch, null, 2), writeFileSync
   - `readSidecar(calmPath: string): object | null` — reads `.calmstudio.json` alongside the .calm file; returns null if missing
   - `writeSidecar(calmPath: string, data: object): void` — writes `.calmstudio.json` alongside the .calm file
   - Import from `node:fs` and `node:path`. Import CalmArchitecture from `@calmstudio/calm-core`.
   - SPDX header on file.

2. Create `packages/mcp-server/src/validation.ts`:
   - `interface ValidationIssue { severity: 'error' | 'warning'; message: string; nodeId?: string; relationshipId?: string; }`
   - `validateArchitecture(arch: CalmArchitecture): ValidationIssue[]` — pure function:
     - Error: node missing `unique-id` or `name`
     - Error: relationship missing `unique-id`, `source`, or `destination`
     - Error: relationship references node not in `arch.nodes` (dangling ref — list available IDs in message)
     - Error: duplicate `unique-id` across nodes
     - Error: duplicate `unique-id` across relationships
     - Warning: node has no relationships (orphan)
     - Warning: relationship `source` equals `destination` (self-loop)
   - Import CalmArchitecture, CalmNode, CalmRelationship from `@calmstudio/calm-core`.
   - SPDX header.

3. Create test stub files — each with `describe` block, `it.todo()` for each behavior, and one trivial passing test (`it('module loads', () => expect(true).toBe(true))`):
   - `createArchitecture.test.ts`: todos for "creates valid architecture from node/rel input", "rejects missing required fields", "uses default file path", "overwrites existing file"
   - `nodes.test.ts`: todos for "add_node appends and writes", "get_node returns matching node", "update_node modifies fields", "delete_node removes node and its relationships", "query_nodes filters by type", "batch_create_nodes adds multiple"
   - `relationships.test.ts`: todos for "add_relationship appends and writes", "rejects dangling source ref", "rejects dangling destination ref", "delete_relationship removes"
   - `validate.test.ts`: todos for "returns empty for valid architecture", "returns error for dangling ref", "returns error for duplicate IDs", "returns warning for orphan node"
   - `render.test.ts`: todos for "returns SVG string for valid architecture", "handles empty architecture"
   - `io.test.ts`: todos for "export_calm writes to path", "import_calm reads and parses", "readCalmFile auto-inits on missing file", "writeCalmFile creates valid JSON"
   - All files have SPDX headers.

4. Update `packages/mcp-server/src/index.ts` to re-export types for now: `export * from './types.js'; export * from './file-io.js'; export * from './validation.js';` (will become the CLI entry point in Plan 02).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/mcp test</automated>
  </verify>
  <done>All 6 test files run with trivial passes, file-io.ts and validation.ts compile, test stubs document all expected behaviors as todos</done>
</task>

</tasks>

<verification>
- `pnpm --filter @calmstudio/mcp build` compiles to dist/ without errors
- `pnpm --filter @calmstudio/mcp test` runs all stub tests and passes
- `packages/mcp-server/package.json` has name `@calmstudio/mcp`, bin field, correct deps
- `packages/mcp-server/tsconfig.json` has `moduleResolution: Node16`
- All Zod schemas exported from types.ts
- file-io.ts exports readCalmFile, writeCalmFile, resolveFile
- validation.ts exports validateArchitecture
</verification>

<success_criteria>
Package builds cleanly, all test stubs pass, Zod schemas define contracts for every MCP tool, file I/O and validation layers are ready for tool handlers to use.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-server/05-00-SUMMARY.md`
</output>
