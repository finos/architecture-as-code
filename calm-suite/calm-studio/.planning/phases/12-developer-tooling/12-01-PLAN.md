---
phase: 12-developer-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-server/src/tools/render.ts
  - packages/mcp-server/src/tests/render.test.ts
  - packages/vscode-extension/package.json
  - packages/vscode-extension/tsconfig.json
  - packages/vscode-extension/esbuild.mjs
  - packages/vscode-extension/src/extension.ts
  - packages/vscode-extension/src/preview.ts
  - packages/vscode-extension/src/test/preview.test.ts
  - packages/vscode-extension/.vscodeignore
  - packages/github-action/package.json
  - packages/github-action/tsconfig.json
  - packages/github-action/action.yml
autonomous: true
requirements:
  - VSCE-01
  - VSCE-02

must_haves:
  truths:
    - "renderArchitectureToSvg(arch) returns valid SVG without reading any file from disk"
    - "VS Code extension package scaffolded with esbuild CJS bundle that compiles without errors"
    - "Opening a .calm.json file in VS Code shows a read-only SVG preview in a side panel"
    - "Saving a .calm.json file causes the preview panel to re-render automatically"
  artifacts:
    - path: "packages/mcp-server/src/tools/render.ts"
      provides: "renderArchitectureToSvg pure function export"
      exports: ["renderArchitectureToSvg", "renderDiagram", "validateArchitectureTool", "registerRenderTools"]
    - path: "packages/vscode-extension/package.json"
      provides: "VS Code extension manifest with commands and activation events"
      contains: "opsflow.calmstudio"
    - path: "packages/vscode-extension/src/extension.ts"
      provides: "Extension activate/deactivate entry point"
      exports: ["activate", "deactivate"]
    - path: "packages/vscode-extension/src/preview.ts"
      provides: "CalmPreviewPanel webview panel with singleton pattern"
      exports: ["CalmPreviewPanel"]
    - path: "packages/github-action/action.yml"
      provides: "GitHub Action metadata file"
      contains: "node20"
  key_links:
    - from: "packages/vscode-extension/src/preview.ts"
      to: "packages/mcp-server/src/tools/render.ts"
      via: "imports renderArchitectureToSvg"
      pattern: "renderArchitectureToSvg"
    - from: "packages/vscode-extension/src/extension.ts"
      to: "packages/vscode-extension/src/preview.ts"
      via: "onDidSaveTextDocument triggers CalmPreviewPanel.updateIfVisible"
      pattern: "onDidSaveTextDocument.*CalmPreviewPanel"
---

<objective>
Extract the pure SVG render function from MCP server, scaffold both new packages, and implement the VS Code preview panel with auto-update on save.

Purpose: Create the shared rendering foundation and the core VS Code extension feature -- live CALM diagram preview that updates on file save. This is the highest-value deliverable of Phase 12.

Output: Refactored render.ts with `renderArchitectureToSvg` export, complete VS Code extension with preview panel, skeleton GitHub Action package ready for Plan 02.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-developer-tooling/12-CONTEXT.md
@.planning/phases/12-developer-tooling/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/mcp-server/src/tools/render.ts (lines 81-208):
```typescript
// Current renderDiagram takes file-based args — extract the pure logic into renderArchitectureToSvg
export async function renderDiagram(args: RenderDiagramArgs): Promise<ToolResponse> {
  // reads file, parses, builds ELK graph, generates SVG
}

// NODE_TYPE_FILL map and escapeXml helper are internal — must remain accessible to new function
const NODE_TYPE_FILL: Record<string, string> = { /* ... */ };
function escapeXml(str: string): string { /* ... */ }
```

From packages/mcp-server/src/validation.ts:
```typescript
export { validateCalmArchitecture as validateArchitecture } from '@calmstudio/calm-core';
export type { ValidationIssue } from '@calmstudio/calm-core';
export type { CalmArchitecture, CalmNode, CalmRelationship } from '@calmstudio/calm-core';
```

From @calmstudio/calm-core (CalmArchitecture type):
```typescript
interface CalmArchitecture {
  nodes: CalmNode[];
  relationships: CalmRelationship[];
}
```

From packages/mcp-server/src/types.ts:
```typescript
export type ToolResponse = { content: ToolContent[]; isError: boolean };
export function toolSuccess(text: string): ToolResponse;
export function toolError(text: string): ToolResponse;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract renderArchitectureToSvg pure function from render.ts</name>
  <files>packages/mcp-server/src/tools/render.ts, packages/mcp-server/src/tests/render.test.ts</files>
  <behavior>
    - Test: renderArchitectureToSvg(sampleArch) returns string containing `<svg` and `</svg>`
    - Test: renderArchitectureToSvg(sampleArch) SVG contains node names "Frontend", "API", "DB"
    - Test: renderArchitectureToSvg(sampleArch, 'RIGHT') returns SVG without error
    - Test: renderArchitectureToSvg(emptyArch) returns placeholder SVG with "No nodes" text
    - Test: existing renderDiagram(file-based) tests still pass unchanged
  </behavior>
  <action>
    Extract the pure ELK layout + SVG generation logic from `renderDiagram()` into a new exported function:

    ```typescript
    export async function renderArchitectureToSvg(
      arch: CalmArchitecture,
      direction: 'DOWN' | 'RIGHT' | 'UP' = 'DOWN'
    ): Promise<string>
    ```

    This function takes a `CalmArchitecture` object directly (no file I/O) and returns the SVG string. Move lines 93-208 of the current `renderDiagram()` into this function. Then refactor `renderDiagram()` to be a thin wrapper:

    ```typescript
    export async function renderDiagram(args: RenderDiagramArgs): Promise<ToolResponse> {
      const filePath = resolveFile(args.file);
      const direction = args.direction ?? 'DOWN';
      let arch: CalmArchitecture;
      try { arch = readCalmFile(filePath); } catch (err) { return toolError(...); }
      const svg = await renderArchitectureToSvg(arch, direction);
      return toolSuccess(svg);
    }
    ```

    The empty architecture check (returning placeholder SVG) should be inside `renderArchitectureToSvg`, not in the wrapper.

    Add new tests in render.test.ts for `renderArchitectureToSvg` that call it directly with in-memory objects (no file I/O). Keep all existing file-based `renderDiagram` and `validateArchitectureTool` tests unchanged.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/mcp test</automated>
  </verify>
  <done>renderArchitectureToSvg is exported from render.ts, takes CalmArchitecture + direction, returns SVG string. All existing render.test.ts tests pass. New pure-function tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold VS Code extension and GitHub Action packages</name>
  <files>packages/vscode-extension/package.json, packages/vscode-extension/tsconfig.json, packages/vscode-extension/esbuild.mjs, packages/vscode-extension/.vscodeignore, packages/vscode-extension/src/extension.ts, packages/github-action/package.json, packages/github-action/tsconfig.json, packages/github-action/action.yml, packages/github-action/src/index.ts</files>
  <action>
    Create two new monorepo packages. Both are already covered by `packages/*` in pnpm-workspace.yaml.

    **packages/vscode-extension/package.json:**
    - `"name": "@calmstudio/vscode-extension"` (internal name, not published to npm)
    - `"publisher": "opsflow"`, `"displayName": "CalmStudio"`, `"description": "CALM architecture diagram preview and MCP integration"`
    - `"version": "0.0.1"`, `"engines": { "vscode": "^1.99.0" }`
    - `"categories": ["Visualization"]`
    - `"main": "./dist/extension.js"` (CJS bundle)
    - `"activationEvents": ["workspaceContains:**/*.calm.json"]`
    - `"contributes.commands"`: `calmstudio.openPreview` ("Open CALM Diagram Preview"), `calmstudio.openInApp` ("Open in CalmStudio")
    - `"contributes.menus.editor/title"`: show openPreview button when `resourceExtname == .json`
    - Leave `contributes.mcpServerDefinitionProviders` out for now (Plan 03)
    - Scripts: `"vscode:prepublish": "node esbuild.mjs --production"`, `"build": "node esbuild.mjs"`, `"test": "vitest run"`, `"typecheck": "tsc --noEmit"`, `"package": "vsce package --no-dependencies"`
    - Dependencies: `"@calmstudio/mcp": "workspace:*"` (for renderArchitectureToSvg import)
    - DevDependencies: `"@types/vscode": "^1.99.0"`, `"@vscode/vsce": "latest"`, `"esbuild": "^0.25.0"`, `"typescript": "^5.7.0"`, `"vitest": "^3.0.8"`
    - Do NOT set `"type": "module"` — VS Code extensions must be CJS

    **packages/vscode-extension/tsconfig.json:**
    - `"target": "ES2022"`, `"module": "CommonJS"`, `"outDir": "./dist"`, `"rootDir": "./src"`
    - `"strict": true`, `"esModuleInterop": true`, `"skipLibCheck": true`
    - `"lib": ["ES2022"]`

    **packages/vscode-extension/esbuild.mjs:**
    - Use the esbuild pattern from RESEARCH Pattern 5 exactly
    - Two entry points: `src/extension.ts` -> `dist/extension.js` (CJS, external: ['vscode'])
    - MCP server bundle entry point will be added in Plan 03
    - Production mode via `--production` flag (minify, no sourcemap)

    **packages/vscode-extension/.vscodeignore:**
    - Ignore `src/`, `node_modules/`, `*.ts` (except `*.d.ts`), `.gitignore`, `tsconfig.json`, `esbuild.mjs`
    - Keep `dist/`, `package.json`, `assets/`

    **packages/vscode-extension/src/extension.ts:**
    - Stub `activate()` and `deactivate()` — activate registers commands and listeners (will be implemented in Task 3)
    - Export both functions

    **packages/github-action/package.json:**
    - `"name": "@calmstudio/github-action"`, `"version": "0.0.1"`, `"private": true`
    - Do NOT set `"type": "module"` (ncc outputs CJS)
    - Dependencies: `"@actions/core": "^1.10.0"`, `"@actions/github": "^6.0.0"`, `"@calmstudio/mcp": "workspace:*"`, `"@calmstudio/calm-core": "workspace:*"`
    - DevDependencies: `"@vercel/ncc": "^0.38.0"`, `"typescript": "^5.7.0"`, `"vitest": "^3.0.8"`
    - Scripts: `"build": "ncc build src/index.ts --out dist --no-source-map"`, `"test": "vitest run"`, `"typecheck": "tsc --noEmit"`

    **packages/github-action/tsconfig.json:**
    - `"target": "ES2022"`, `"module": "CommonJS"`, `"outDir": "./dist"`, `"rootDir": "./src"`
    - `"strict": true`, `"esModuleInterop": true`

    **packages/github-action/action.yml:**
    - Use the action.yml from RESEARCH Pattern 6 exactly
    - Name: `CalmStudio Diagram Renderer`
    - Inputs: `github-token` (default: `${{ github.token }}`), `calm-files` (default: `**/*.calm.json`)
    - Outputs: `comment-url`
    - `runs: using: node20, main: dist/index.js`

    **packages/github-action/src/index.ts:**
    - Minimal stub: `import * as core from '@actions/core'; async function run() { core.info('CalmStudio GitHub Action'); } run().catch(e => core.setFailed(String(e)));`

    After creating files, run `pnpm install` to link workspace packages, then verify both packages build:
    - `cd packages/vscode-extension && node esbuild.mjs` (should produce dist/extension.js)
    - VS Code extension typecheck: `pnpm --filter @calmstudio/vscode-extension typecheck`
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm install && pnpm --filter @calmstudio/vscode-extension build && pnpm --filter @calmstudio/vscode-extension typecheck</automated>
  </verify>
  <done>Both packages exist in monorepo, pnpm install links them, VS Code extension builds to dist/extension.js via esbuild, typecheck passes, GitHub Action package.json and action.yml are valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement CalmPreviewPanel with auto-update on save</name>
  <files>packages/vscode-extension/src/preview.ts, packages/vscode-extension/src/extension.ts, packages/vscode-extension/src/test/preview.test.ts, packages/vscode-extension/vitest.config.ts</files>
  <behavior>
    - Test: getWebviewContent(svgString) returns HTML containing the SVG string, DOCTYPE, and CSP meta tag
    - Test: getWebviewContent wraps SVG in body with VS Code-compatible CSS (var(--vscode-editor-background))
    - Test: isCalmFile returns true for paths ending in .calm.json and .json
    - Test: isCalmFile returns false for paths ending in .ts, .md, .txt
    - Test: getNonce returns a 32-character alphanumeric string
  </behavior>
  <action>
    **packages/vscode-extension/vitest.config.ts:**
    - Standard vitest config, `test: { include: ['src/test/**/*.test.ts'] }`
    - No special environment needed (pure function tests, no VS Code API in unit tests)

    **packages/vscode-extension/src/preview.ts:**
    Implement `CalmPreviewPanel` class following RESEARCH Pattern 1 exactly:
    - Static singleton `currentPanel` pattern — `createOrShow(context, uri)` reuses existing panel or creates new one
    - `_updateForUri(uri)`: read file via `vscode.workspace.fs.readFile`, parse JSON, call `renderArchitectureToSvg(arch)` from `@calmstudio/mcp` tools/render.ts, set `panel.webview.html = getWebviewContent(svg, nonce)`
    - `updateIfVisible(uri)`: static method that updates the panel if it exists and is visible (called from save listener)
    - On panel dispose: set `currentPanel = undefined`, dispose all subscriptions
    - Panel options: `enableScripts: false` (static SVG only), `localResourceRoots: []`
    - Panel title: `'CALM Preview'`, viewColumn: `Beside` with `preserveFocus: true`

    Export `getWebviewContent(svg: string, nonce: string): string` as a named export (for testing).
    Export `isCalmFile(uri: vscode.Uri): boolean` — returns true if path ends with `.calm.json` or `.json`.
    Export `getNonce(): string` — 32-char random alphanumeric.

    `getWebviewContent` uses CSP-compliant HTML from RESEARCH code example — `default-src 'none'; img-src data:; style-src 'nonce-${nonce}'`. Body background uses `var(--vscode-editor-background)`. SVG is inserted directly (not via img tag — this is webview HTML, not GitHub markdown).

    **packages/vscode-extension/src/extension.ts:**
    Implement `activate(context)`:
    1. Register `calmstudio.openPreview` command: `CalmPreviewPanel.createOrShow(context, activeEditor.document.uri)`
    2. Register `onDidSaveTextDocument` listener (RESEARCH Pattern 2): filter for `.calm.json`/`.json`, call `CalmPreviewPanel.updateIfVisible(doc.uri)`
    3. Register `onDidChangeActiveTextEditor` listener: if editor has a calm file, auto-open preview panel (per user decision: "Preview auto-opens when a .calm.json file is the active editor")
    4. Push all disposables to `context.subscriptions`

    `deactivate()`: empty function (VS Code handles cleanup via subscriptions).

    Import `renderArchitectureToSvg` from the mcp-server package. Since the extension bundles with esbuild, this import will be resolved at bundle time:
    ```typescript
    import { renderArchitectureToSvg } from '@calmstudio/mcp/src/tools/render.js';
    ```
    Note: esbuild resolves workspace imports. If this path doesn't work with CJS output, use a relative path through node_modules or adjust the esbuild config to alias it.

    **packages/vscode-extension/src/test/preview.test.ts:**
    Test the pure exported functions (getWebviewContent, isCalmFile, getNonce) without VS Code API.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/vscode-extension test && pnpm --filter @calmstudio/vscode-extension build</automated>
  </verify>
  <done>CalmPreviewPanel renders CALM JSON as SVG in a webview panel. onDidSaveTextDocument triggers re-render for .calm.json files. onDidChangeActiveTextEditor auto-opens preview. Pure function unit tests pass. Extension builds to dist/extension.js without errors.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @calmstudio/mcp test` — all existing MCP server tests pass, new renderArchitectureToSvg tests pass
2. `pnpm --filter @calmstudio/vscode-extension test` — preview panel pure function tests pass
3. `pnpm --filter @calmstudio/vscode-extension build` — esbuild produces dist/extension.js (CJS)
4. `pnpm --filter @calmstudio/vscode-extension typecheck` — no TypeScript errors
5. `grep "renderArchitectureToSvg" packages/mcp-server/src/tools/render.ts` — function is exported
6. `grep "renderArchitectureToSvg" packages/vscode-extension/src/preview.ts` — function is imported
</verification>

<success_criteria>
- renderArchitectureToSvg pure function exists and is tested with in-memory CalmArchitecture objects
- VS Code extension scaffold compiles, bundles, and typechecks
- Preview panel implementation uses singleton pattern, CSP-compliant webview HTML
- Save listener filters for .calm.json files and triggers preview update
- Active editor change listener auto-opens preview for CALM files
- GitHub Action package skeleton exists with valid action.yml
</success_criteria>

<output>
After completion, create `.planning/phases/12-developer-tooling/12-01-SUMMARY.md`
</output>
