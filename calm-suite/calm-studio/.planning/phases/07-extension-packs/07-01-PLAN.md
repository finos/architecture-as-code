---
phase: 07-extension-packs
plan: 01
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - packages/extensions/src/packs/aws.ts
  - packages/extensions/src/packs/gcp.ts
  - packages/extensions/src/packs/azure.ts
  - packages/extensions/src/packs/kubernetes.ts
  - packages/extensions/src/packs/ai.ts
  - packages/extensions/src/icons/aws.ts
  - packages/extensions/src/icons/gcp.ts
  - packages/extensions/src/icons/azure.ts
  - packages/extensions/src/icons/k8s.ts
  - packages/extensions/src/icons/ai.ts
  - packages/extensions/src/index.ts
  - packages/extensions/src/registry.test.ts
autonomous: true
requirements:
  - EXTK-03
  - EXTK-04
  - EXTK-05
  - EXTK-06
  - EXTK-07

must_haves:
  truths:
    - "AWS pack has 30+ service types with distinct SVG icons and orange color family"
    - "GCP pack has 15+ service types with GCP multi-color family"
    - "Azure pack has 15+ service types with blue color family and hand-crafted abstract icons"
    - "Kubernetes pack has 14+ resource types with blue color family"
    - "AI/Agentic pack has 14+ types with custom icons"
    - "All pack nodes use colon-prefixed typeIds (aws:lambda, k8s:pod, etc.)"
    - "initAllPacks() registers all 6 packs (core + 5 extension)"
  artifacts:
    - path: "packages/extensions/src/packs/aws.ts"
      provides: "AWS pack definition with 30+ types"
      exports: ["awsPack"]
    - path: "packages/extensions/src/packs/kubernetes.ts"
      provides: "Kubernetes pack definition with 14+ types"
      exports: ["kubernetesPack"]
    - path: "packages/extensions/src/packs/ai.ts"
      provides: "AI/Agentic pack definition with 14+ types"
      exports: ["aiPack"]
    - path: "packages/extensions/src/packs/gcp.ts"
      provides: "GCP pack definition with 15+ types"
      exports: ["gcpPack"]
    - path: "packages/extensions/src/packs/azure.ts"
      provides: "Azure pack definition with 15+ types"
      exports: ["azurePack"]
  key_links:
    - from: "packages/extensions/src/index.ts"
      to: "packages/extensions/src/packs/*.ts"
      via: "initAllPacks() registers all packs"
      pattern: "registerPack\\((aws|gcp|azure|kubernetes|ai)Pack\\)"
---

<objective>
Create all 5 extension pack definitions (AWS, GCP, Azure, Kubernetes, AI/Agentic) with hand-crafted SVG icons and provider-branded color families, then wire them into initAllPacks().

Purpose: Populates the extension pack registry with all domain-specific node types that architects need for cloud/AI diagramming.
Output: 5 pack definition files with icons, all registered via initAllPacks().
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-extension-packs/07-CONTEXT.md
@.planning/phases/07-extension-packs/07-RESEARCH.md
@.planning/phases/07-extension-packs/07-00-SUMMARY.md

<interfaces>
<!-- From Plan 00 output — the type contracts -->

From packages/extensions/src/types.ts:
```typescript
export interface PackColor {
  bg: string;
  border: string;
  stroke: string;
  badge?: string;
}

export interface NodeTypeEntry {
  typeId: string;    // e.g. 'aws:lambda'
  label: string;     // e.g. 'Lambda'
  icon: string;      // Inline SVG string
  color: PackColor;
  description?: string;
}

export interface PackDefinition {
  id: string;        // 'aws', 'gcp', 'k8s', 'azure', 'ai', 'core'
  label: string;     // 'AWS', 'GCP', etc.
  version: string;
  color: PackColor;  // Pack-level default
  nodes: NodeTypeEntry[];
}
```

From packages/extensions/src/registry.ts:
```typescript
export function registerPack(pack: PackDefinition): void;
export function resolvePackNode(calmType: string): NodeTypeEntry | null;
export function getAllPacks(): PackDefinition[];
export function getPacksForTypes(calmTypes: string[]): string[];
export function resetRegistry(): void;
```

From packages/extensions/src/index.ts:
```typescript
export function initAllPacks(): void;  // currently registers only corePack
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AWS, Kubernetes, and AI/Agentic pack definitions with icons</name>
  <files>
    packages/extensions/src/icons/aws.ts,
    packages/extensions/src/icons/k8s.ts,
    packages/extensions/src/icons/ai.ts,
    packages/extensions/src/packs/aws.ts,
    packages/extensions/src/packs/kubernetes.ts,
    packages/extensions/src/packs/ai.ts
  </files>
  <action>
    Create icon files and pack definitions for the 3 largest/most-important packs:

    **Icon files:** Each `icons/{pack}.ts` exports a record of SVG string constants. All icons must be hand-crafted abstract SVGs (16x16 viewBox, stroke-based) to avoid licensing issues. Icons should be visually representative of the service (e.g., Lambda = function/arrow, S3 = bucket, DynamoDB = table rows, EKS = container cluster).

    **AWS Pack** (`src/packs/aws.ts`): Export `awsPack: PackDefinition` with:
    - id: 'aws', label: 'AWS', version: '1.0.0'
    - Color family: bg '#fff8f0', border '#ff9900', stroke '#e67e00', badge '[AWS]'
    - 30+ services with colon-prefixed typeIds. Required types: aws:lambda, aws:s3, aws:dynamodb, aws:ecs, aws:eks, aws:sqs, aws:sns, aws:api-gateway, aws:rds, aws:aurora, aws:cloudfront, aws:route53, aws:iam, aws:vpc, aws:ec2, aws:fargate, aws:eventbridge, aws:step-functions, aws:cognito, aws:elasticache, aws:kinesis, aws:redshift, aws:sagemaker, aws:glue, aws:secrets-manager, aws:cloudwatch, aws:waf, aws:kms, aws:elb, aws:efs

    **Kubernetes Pack** (`src/packs/kubernetes.ts`): Export `kubernetesPack: PackDefinition` with:
    - id: 'k8s', label: 'Kubernetes', version: '1.0.0'
    - Color family: bg '#f0f4ff', border '#326ce5', stroke '#2054c8', badge '[K8s]'
    - ~15 types: k8s:pod, k8s:deployment, k8s:statefulset, k8s:daemonset, k8s:job, k8s:cronjob, k8s:service, k8s:ingress, k8s:configmap, k8s:secret, k8s:persistent-volume, k8s:pvc, k8s:namespace, k8s:hpa

    **AI/Agentic Pack** (`src/packs/ai.ts`): Export `aiPack: PackDefinition` with:
    - id: 'ai', label: 'AI / Agentic', version: '1.0.0'
    - Color family: bg '#f5f0ff', border '#8b5cf6', stroke '#7c3aed', badge '[AI]'
    - ~14 types: ai:llm, ai:agent, ai:orchestrator, ai:vector-store, ai:tool, ai:memory, ai:guardrail, ai:embedding-model, ai:rag-pipeline, ai:prompt-template, ai:api-gateway, ai:human-in-the-loop, ai:knowledge-base, ai:eval-monitor

    Each node entry gets: typeId, label (human-readable), icon (SVG string from icons file), color (can override pack default or use it), description (1-line).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/extensions tsc --noEmit</automated>
  </verify>
  <done>AWS pack has 30+ entries, K8s pack has 14+ entries, AI pack has 14+ entries, all with SVG icons and colon-prefixed typeIds, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: GCP, Azure packs + wire initAllPacks + tests</name>
  <files>
    packages/extensions/src/icons/gcp.ts,
    packages/extensions/src/icons/azure.ts,
    packages/extensions/src/packs/gcp.ts,
    packages/extensions/src/packs/azure.ts,
    packages/extensions/src/index.ts,
    packages/extensions/src/registry.test.ts
  </files>
  <action>
    **GCP Pack** (`src/packs/gcp.ts`): Export `gcpPack: PackDefinition` with:
    - id: 'gcp', label: 'GCP', version: '1.0.0'
    - Color family: bg '#f0f9f4', border '#34a853', stroke '#1e8e3e', badge '[GCP]'
    - 15 types: gcp:cloud-run, gcp:cloud-functions, gcp:gke, gcp:cloud-sql, gcp:bigquery, gcp:pub-sub, gcp:cloud-storage, gcp:firestore, gcp:spanner, gcp:cloud-cdn, gcp:cloud-dns, gcp:cloud-armor, gcp:vertex-ai, gcp:cloud-endpoints, gcp:memorystore

    **Azure Pack** (`src/packs/azure.ts`): Export `azurePack: PackDefinition` with:
    - id: 'azure', label: 'Azure', version: '1.0.0'
    - Color family: bg '#f0f5ff', border '#0078d4', stroke '#005a9e', badge '[Azure]'
    - Hand-crafted ABSTRACT icons only (NOT Microsoft official icons — per RESEARCH licensing constraint)
    - 15 types: azure:functions, azure:app-service, azure:aks, azure:sql-database, azure:cosmos-db, azure:service-bus, azure:blob-storage, azure:front-door, azure:api-management, azure:key-vault, azure:active-directory, azure:cognitive-services, azure:event-hub, azure:redis-cache, azure:container-instances

    **Update `src/index.ts`:** Add imports for all 5 extension packs and update `initAllPacks()` to register all 6 packs (core + aws + gcp + azure + kubernetes + ai).

    **Update `src/registry.test.ts`:** Add tests verifying:
    - AWS pack has >= 30 node entries
    - GCP pack has >= 15 node entries
    - Azure pack has >= 15 node entries
    - K8s pack has >= 14 node entries
    - AI pack has >= 14 node entries
    - After initAllPacks(), resolvePackNode('aws:lambda') returns entry with label 'Lambda'
    - After initAllPacks(), resolvePackNode('k8s:pod') returns entry with label 'Pod'
    - After initAllPacks(), resolvePackNode('ai:agent') returns entry with label 'Agent'
    - After initAllPacks(), resolvePackNode('gcp:cloud-run') returns entry with label 'Cloud Run'
    - After initAllPacks(), resolvePackNode('azure:functions') returns entry with label 'Functions'
    - getAllPacks() returns 6 packs after initAllPacks()
    - Every pack node has non-empty icon string
    - Every pack node has a valid PackColor (bg, border, stroke all non-empty)
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter @calmstudio/extensions vitest run</automated>
  </verify>
  <done>All 6 packs (core + 5 extension) registered via initAllPacks(), all pack node counts meet requirements, resolvePackNode resolves across all packs, all tests pass</done>
</task>

</tasks>

<verification>
- `pnpm --filter @calmstudio/extensions vitest run` — all pack tests pass
- `pnpm --filter @calmstudio/extensions tsc --noEmit` — no type errors
- Manual count: AWS >= 30, GCP >= 15, Azure >= 15, K8s >= 14, AI >= 14
</verification>

<success_criteria>
- 5 extension packs + 1 core pack all register via initAllPacks()
- AWS has 30+ types, GCP 15+, Azure 15+, K8s 14+, AI 14+
- All node types use colon-prefixed typeIds
- All icons are hand-crafted SVGs (no copyrighted artwork)
- Azure icons are abstract (not Microsoft official) per licensing research
- Provider color families match branding (AWS orange, K8s blue, Azure blue, GCP green, AI purple)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-extension-packs/07-01-SUMMARY.md`
</output>
