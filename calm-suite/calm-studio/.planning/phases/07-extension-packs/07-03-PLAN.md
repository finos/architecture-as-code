---
phase: 07-extension-packs
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - apps/studio/src/routes/+page.svelte
  - apps/studio/src/lib/io/fileSystem.ts
  - apps/studio/src/lib/io/export.ts
  - apps/studio/src/tests/projection.test.ts
autonomous: false
requirements:
  - EXTK-01
  - EXTK-08

must_haves:
  truths:
    - "initAllPacks() is called at app startup so all packs are available before any rendering"
    - "Extension pack nodes round-trip through CALM JSON export and import without data loss"
    - "Missing sidecar banner appears when opening CALM JSON with pack-prefixed types"
    - "Diagrams with only core CALM types do not create .calmstudio.json sidecar"
    - "Extension pack node on canvas shows correct icon, color, and label after visual verification"
  artifacts:
    - path: "apps/studio/src/routes/+page.svelte"
      provides: "App startup wiring for initAllPacks()"
      contains: "initAllPacks"
    - path: "apps/studio/src/tests/projection.test.ts"
      provides: "Extended projection tests for pack-prefixed types"
      contains: "extension"
  key_links:
    - from: "apps/studio/src/routes/+page.svelte"
      to: "@calmstudio/extensions"
      via: "initAllPacks() call at module level"
      pattern: "initAllPacks"
    - from: "apps/studio/src/lib/stores/projection.ts"
      to: "apps/studio/src/lib/canvas/nodeTypes.ts"
      via: "resolveNodeType now returns 'extension' for pack types"
      pattern: "resolveNodeType"
---

<objective>
Wire extension packs into app startup, add projection round-trip tests, integrate sidecar awareness into file I/O, and visually verify the complete extension pack system.

Purpose: Final integration and visual verification that the full extension pack system works end-to-end.
Output: Complete working extension pack system with visual verification.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-extension-packs/07-CONTEXT.md
@.planning/phases/07-extension-packs/07-RESEARCH.md
@.planning/phases/07-extension-packs/07-00-SUMMARY.md
@.planning/phases/07-extension-packs/07-01-SUMMARY.md
@.planning/phases/07-extension-packs/07-02-SUMMARY.md

<interfaces>
From packages/extensions/src/index.ts:
```typescript
export function initAllPacks(): void;
export function resolvePackNode(calmType: string): NodeTypeEntry | null;
export function getAllPacks(): PackDefinition[];
export function getPacksForTypes(calmTypes: string[]): string[];
```

From apps/studio/src/lib/io/sidecar.ts:
```typescript
export function sidecarNameFor(diagramName: string): string;
export function detectPacksFromArch(arch: { nodes: Array<{ 'node-type': string }> }): string[];
export function buildSidecarData(packIds: string[]): SidecarData;
```

From apps/studio/src/lib/stores/projection.ts:
```typescript
export function calmToFlow(arch, positionMap?): { nodes: Node[]; edges: Edge[] };
export function flowToCalm(nodes, edges): CalmArchitecture;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App startup wiring + projection tests + sidecar-aware file I/O</name>
  <files>
    apps/studio/src/routes/+page.svelte,
    apps/studio/src/lib/io/fileSystem.ts,
    apps/studio/src/lib/io/export.ts,
    apps/studio/src/tests/projection.test.ts
  </files>
  <action>
    1. **+page.svelte:** Add `import { initAllPacks } from '@calmstudio/extensions'` at the top of the script. Call `initAllPacks()` at module level (not inside onMount) so packs are registered before any component renders. This follows the RESEARCH recommendation (Pattern 7 pitfall — register at module load, not lazy).

    2. **projection.test.ts:** Extend existing projection tests with pack-aware cases:
       - Import `initAllPacks` from `@calmstudio/extensions` and call it in beforeAll
       - Test: `calmToFlow` with a node having `node-type: 'aws:lambda'` produces a Svelte Flow node with `type: 'extension'` and `data.calmType: 'aws:lambda'`
       - Test: `flowToCalm` with a node having `data.calmType: 'aws:lambda'` preserves `node-type: 'aws:lambda'` in output
       - Test: Round-trip — calmToFlow then flowToCalm preserves `aws:lambda` as node-type string
       - Test: Mixed diagram — nodes with both core types ('service') and pack types ('k8s:pod') resolve correctly

    3. **fileSystem.ts:** Add sidecar awareness to import flow:
       - After importing a CALM JSON file, call `detectPacksFromArch(arch)` from sidecar.ts
       - If pack IDs detected and no sidecar data was provided, set an `extensionPackBanner` flag/state that +page.svelte can read to show the info banner: "Extension pack types detected. [Enable packs] [Dismiss]"
       - The banner logic: "Enable packs" calls `initAllPacks()` (already done) and re-renders; "Dismiss" hides banner. Since initAllPacks runs at startup, "Enable packs" is effectively just dismissing the banner — packs are already loaded.
       - Keep it simple: the banner is informational for v1. All pack types already render correctly via ExtensionNode because initAllPacks() runs at startup.

    4. **export.ts:** When exporting CALM JSON and `detectPacksFromArch()` returns non-empty pack IDs, also generate a `.calmstudio.json` sidecar blob and offer it as a second download. Use `buildSidecarData(packIds)` from sidecar.ts. This is a best-effort enhancement — if File System Access API is available, save sidecar via separate file handle; if using Blob download, trigger a second download for the sidecar file.

    IMPORTANT: Do NOT create the sidecar for diagrams with only core CALM types (detectPacksFromArch returns empty array).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter studio vitest run</automated>
  </verify>
  <done>initAllPacks called at app startup, projection tests pass for pack-prefixed types, sidecar-aware import shows banner, export generates sidecar for pack-using diagrams</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete extension pack system</name>
  <action>
    Verify the complete extension pack system visually in the browser:
    1. Run `cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm --filter studio dev` to start the dev server
    2. Open http://localhost:5173 in a browser
    3. Verify palette shows collapsible sections:
       - "Core CALM" section (expanded by default) — 9 original node types
       - "AWS" section (collapsed) — click to expand, verify 30+ service types
       - "Kubernetes" section — verify 14+ types
       - "AI / Agentic" section — verify 14+ types
       - "GCP" and "Azure" sections — verify 15+ types each
    4. Search for "lambda" — verify result shows "Lambda [AWS]" with badge
    5. Search for "pod" — verify "Pod [K8s]" with badge
    6. Drag "Lambda" from AWS section to canvas — verify:
       - Node renders with orange-branded color (not generic dashed box)
       - Node shows Lambda icon and "Lambda" label
       - Node shows "aws:lambda" type badge
    7. Drag a "Pod" from K8s section — verify blue-branded K8s styling
    8. Drag an "Agent" from AI section — verify purple styling
    9. Drag a core "Service" node — verify it still renders as before (no regression)
    10. Check CALM JSON in code panel — verify pack nodes have colon-prefixed node-type: "aws:lambda", "k8s:pod", etc.
    11. Export as CALM JSON — verify the exported file is valid JSON
    12. Close and re-import the exported file — verify all nodes render correctly
  </action>
  <verify>User visually confirms extension pack rendering, palette sections, search badges, and CALM JSON round-trip</verify>
  <done>User has approved the visual output of the complete extension pack system</done>
</task>

</tasks>

<verification>
- `pnpm --filter studio vitest run` — all tests pass
- `pnpm --filter @calmstudio/extensions vitest run` — all extension tests pass
- Visual: Pack nodes render with correct icons, colors, and labels on canvas
- Visual: Palette has 6 collapsible sections with search badges
- Functional: CALM JSON round-trip preserves pack-prefixed node types
</verification>

<success_criteria>
- Extension pack nodes are visually distinct on canvas (icon + color per provider)
- Palette organizes 100+ node types across 6 collapsible sections
- Search works across all packs with badge attribution
- CALM JSON export/import round-trips correctly for pack-prefixed types
- No regression in core CALM node rendering or editing
- Sidecar file generated only when extension pack nodes are present
- User has visually verified the complete system
</success_criteria>

<output>
After completion, create `.planning/phases/07-extension-packs/07-03-SUMMARY.md`
</output>
