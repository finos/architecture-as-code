---
phase: 01-foundation-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .npmrc
  - apps/studio/package.json
  - apps/studio/tsconfig.json
  - apps/studio/src/index.ts
  - packages/calm-core/package.json
  - packages/calm-core/tsconfig.json
  - packages/calm-core/src/index.ts
  - packages/calmscript/package.json
  - packages/calmscript/tsconfig.json
  - packages/calmscript/src/index.ts
  - packages/mcp-server/package.json
  - packages/mcp-server/tsconfig.json
  - packages/mcp-server/src/index.ts
  - packages/extensions/package.json
  - packages/extensions/tsconfig.json
  - packages/extensions/src/index.ts
  - LICENSE
  - LICENSES/Apache-2.0.txt
  - .reuse/dep5
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - NOTICE
  - MAINTAINERS.md
  - README.md
  - commitlint.config.cjs
  - .husky/commit-msg
autonomous: true
requirements:
  - GOVN-01
  - GOVN-02
  - GOVN-03
  - GOVN-04
  - GOVN-05
  - GOVN-06
  - GOVN-07
  - GOVN-08
  - CICD-05

must_haves:
  truths:
    - "pnpm install succeeds and resolves all workspace packages"
    - "Every .ts, .cjs, and future .svelte source file carries SPDX Apache-2.0 headers"
    - "reuse lint passes with zero non-compliant files"
    - "All five FINOS governance files exist and are linked from README"
    - "commitlint rejects a non-conventional commit message via husky hook"
    - "TypeScript strict mode compiles across all packages with zero errors"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package declarations"
      contains: "packages"
    - path: "package.json"
      provides: "Root workspace package with scripts"
      contains: "private"
    - path: "tsconfig.base.json"
      provides: "Shared strict TypeScript config"
      contains: "noUncheckedIndexedAccess"
    - path: "CONTRIBUTING.md"
      provides: "Contributor workflow with DCO instructions"
      contains: "Signed-off-by"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant v2.1"
      contains: "Contributor Covenant"
    - path: "SECURITY.md"
      provides: "Vulnerability disclosure policy"
      contains: "Security Advisories"
    - path: "NOTICE"
      provides: "Third-party attribution"
      contains: "FINOS"
    - path: "MAINTAINERS.md"
      provides: "Project governance structure"
      contains: "Maintainers"
    - path: ".reuse/dep5"
      provides: "SPDX coverage for binary/config files"
      contains: "Apache-2.0"
    - path: "commitlint.config.cjs"
      provides: "Conventional commit rules with scope enum"
      contains: "scope-enum"
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "pnpm workspace resolution"
      pattern: "workspace"
    - from: "apps/studio/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig\\.base"
    - from: ".husky/commit-msg"
      to: "commitlint.config.cjs"
      via: "npx commitlint"
      pattern: "commitlint"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "markdown link"
      pattern: "CONTRIBUTING"
---

<objective>
Create the complete pnpm monorepo skeleton with all workspace packages, Apache 2.0 SPDX licensing with REUSE compliance, all five FINOS governance documents, commitlint + husky for conventional commit enforcement, and shared TypeScript strict-mode configuration.

Purpose: Establish the project foundation so every file created from Phase 2 onward inherits proper licensing, governance, commit conventions, and type safety.
Output: A buildable, lint-passing, REUSE-compliant pnpm workspace with governance files linked from README.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-governance/01-CONTEXT.md
@.planning/phases/01-foundation-governance/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm monorepo with all workspace packages and TypeScript config</name>
  <files>
    pnpm-workspace.yaml, package.json, tsconfig.base.json, .npmrc,
    apps/studio/package.json, apps/studio/tsconfig.json, apps/studio/src/index.ts,
    packages/calm-core/package.json, packages/calm-core/tsconfig.json, packages/calm-core/src/index.ts,
    packages/calmscript/package.json, packages/calmscript/tsconfig.json, packages/calmscript/src/index.ts,
    packages/mcp-server/package.json, packages/mcp-server/tsconfig.json, packages/mcp-server/src/index.ts,
    packages/extensions/package.json, packages/extensions/tsconfig.json, packages/extensions/src/index.ts
  </files>
  <action>
Create the monorepo skeleton per user decisions:

1. **pnpm-workspace.yaml** with `packages: ['apps/*', 'packages/*']`

2. **Root package.json** — `private: true`, name `calmstudio-workspace`, scripts:
   - `"prepare": "husky || true"` (the `|| true` prevents CI failure per Pitfall 4 from research)
   - `"build": "pnpm -r run build"`
   - `"lint": "pnpm -r run lint"`
   - `"test": "pnpm -r run test"`
   - `"typecheck": "pnpm -r run typecheck"`
   - `engines: { "node": ">=20", "pnpm": ">=9" }`

3. **.npmrc** with `shamefully-hoist=false` and `strict-peer-dependencies=true`

4. **tsconfig.base.json** — strict TypeScript per user decision:
   ```json
   {
     "compilerOptions": {
       "strict": true,
       "noUncheckedIndexedAccess": true,
       "exactOptionalPropertyTypes": true,
       "target": "ES2022",
       "module": "ESNext",
       "moduleResolution": "bundler",
       "esModuleInterop": true,
       "skipLibCheck": true,
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true
     }
   }
   ```

5. **Per-package scaffolds** — For each of `apps/studio`, `packages/calm-core`, `packages/calmscript`, `packages/mcp-server`, `packages/extensions`:
   - `package.json` with `name` as `@calmstudio/{pkg}`, `version: "0.0.0"`, `private: true`, `type: "module"`, minimal scripts (`build`, `lint`, `test`, `typecheck` — all can be `echo "no-op" && exit 0` stubs for now)
   - `tsconfig.json` extending `../../tsconfig.base.json` (or `../tsconfig.base.json` for apps/studio), with `include: ["src"]`, `exclude: ["node_modules", "dist"]`
   - `src/index.ts` — stub export with SPDX header:
     ```typescript
     // SPDX-FileCopyrightText: 2024 CalmStudio contributors - see NOTICE file
     //
     // SPDX-License-Identifier: Apache-2.0
     export {};
     ```

6. **Install dev dependencies at workspace root:**
   ```bash
   pnpm add -D -w typescript husky @commitlint/cli @commitlint/config-conventional
   ```

7. Run `pnpm install` to generate lockfile.

8. Run `pnpm -r run build` to verify all packages resolve (stubs should pass).

IMPORTANT: apps/studio tsconfig.json extends `../../tsconfig.base.json` (two levels up from apps/studio/).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && pnpm install --frozen-lockfile 2>/dev/null || pnpm install && pnpm -r run typecheck</automated>
  </verify>
  <done>pnpm workspace resolves all 5 packages, TypeScript compiles with strict mode across all packages, lockfile exists</done>
</task>

<task type="auto">
  <name>Task 2: Create FINOS governance files, REUSE/SPDX licensing setup, and README</name>
  <files>
    LICENSE, LICENSES/Apache-2.0.txt, .reuse/dep5,
    CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, NOTICE, MAINTAINERS.md, README.md
  </files>
  <action>
1. **LICENSE** — Full Apache 2.0 license text at repo root (for GitHub license detection).

2. **LICENSES/Apache-2.0.txt** — Run `reuse download Apache-2.0` if `reuse` CLI is available, otherwise manually create `LICENSES/Apache-2.0.txt` with the full Apache 2.0 text. This is required by the REUSE spec.

3. **.reuse/dep5** — Debian copyright format covering files that cannot carry inline SPDX headers:
   ```
   Format: https://www.debian.org/doc/packaging-manuals/copyright-format/1.0/
   Upstream-Name: calmstudio
   Upstream-Contact: https://github.com/finos/calmstudio
   Source: https://github.com/finos/calmstudio

   Files: *.json *.yaml *.yml pnpm-lock.yaml .npmrc .gitignore
   Copyright: 2024 CalmStudio contributors - see NOTICE file
   License: Apache-2.0

   Files: *.png *.svg *.ico
   Copyright: 2024 CalmStudio contributors - see NOTICE file
   License: Apache-2.0

   Files: .planning/*
   Copyright: 2024 CalmStudio contributors - see NOTICE file
   License: Apache-2.0

   Files: .husky/*
   Copyright: 2024 CalmStudio contributors - see NOTICE file
   License: Apache-2.0
   ```
   Use `**/*.json` style if `reuse lint` requires recursive matching. Test with `reuse lint` locally and adjust patterns until compliant.

4. **CONTRIBUTING.md** — Per user decision: fork-and-PR process, DCO sign-off instructions (`git commit -s`), conventional commits guide with scope list (studio, desktop, calm-core, calmscript, mcp-server, extensions, ci, docs, deps), local dev setup (`pnpm install`), testing expectations, code review SLA. Include SPDX header at top.

5. **CODE_OF_CONDUCT.md** — Contributor Covenant v2.1 full text. Include SPDX header.

6. **SECURITY.md** — GitHub Security Advisories for private vulnerability reporting, 90-day coordinated disclosure timeline per user decision. Include SPDX header.

7. **NOTICE** — Per FINOS template from research:
   ```
   CalmStudio - FINOS
   Copyright 2024-2025 CalmStudio contributors - see MAINTAINERS.md

   This product includes software developed at
   The Fintech Open Source Foundation (https://www.finos.org/).
   ```

8. **MAINTAINERS.md** — BDFL + committers model per user decision. Initial maintainer list with project lead placeholder. Include SPDX header.

9. **README.md** — Project name, tagline ("Architecture diagrams as code — draw visually, get validated CALM JSON automatically"), badges placeholder, description, quick start, links to all five governance files (CONTRIBUTING, CODE_OF_CONDUCT, SECURITY, NOTICE, MAINTAINERS), license badge. Include SPDX header.

All `.md` files use HTML comment SPDX headers:
```html
<!-- SPDX-FileCopyrightText: 2024 CalmStudio contributors - see NOTICE file -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
```

The NOTICE file uses no SPDX header (it IS the attribution file — covered by dep5 if needed, or add a `.license` sidecar).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && test -f CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && test -f SECURITY.md && test -f NOTICE && test -f MAINTAINERS.md && test -f README.md && test -f LICENSE && test -f LICENSES/Apache-2.0.txt && test -f .reuse/dep5 && (reuse lint 2>/dev/null || echo "reuse CLI not installed — verify manually")</automated>
  </verify>
  <done>All 5 FINOS governance files exist, LICENSE + LICENSES/Apache-2.0.txt exist, .reuse/dep5 covers binary/config files, README links to all governance files, reuse lint passes (or all files have SPDX headers for when it runs in CI)</done>
</task>

<task type="auto">
  <name>Task 3: Configure commitlint + husky for conventional commit enforcement</name>
  <files>commitlint.config.cjs, .husky/commit-msg</files>
  <action>
1. **commitlint.config.cjs** — With SPDX header, extending `@commitlint/config-conventional`, scope-enum rule with the exact scopes from user decision: `['studio', 'desktop', 'calm-core', 'calmscript', 'mcp-server', 'extensions', 'ci', 'docs', 'deps']`.

2. **Initialize husky:**
   ```bash
   pnpm exec husky
   ```
   This creates the `.husky/` directory.

3. **.husky/commit-msg** — Create file with content:
   ```sh
   npx --no -- commitlint --edit $1
   ```
   Make it executable: `chmod +x .husky/commit-msg`

4. **Verify the hook works** by testing commitlint directly:
   ```bash
   echo "bad commit" | pnpm exec commitlint
   # Should fail
   echo "feat(studio): add canvas" | pnpm exec commitlint
   # Should pass
   ```

NOTE: Do NOT use `husky install` (removed in v9) or `husky add` (deprecated in v9). Husky v9 uses the `"prepare": "husky"` script (already in root package.json from Task 1) and direct file creation in `.husky/`.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/excalicalm/calmstudio && echo "bad commit message" | pnpm exec commitlint 2>&1 | grep -q "input" && echo "feat(studio): add canvas component" | pnpm exec commitlint && echo "PASS: commitlint works"</automated>
  </verify>
  <done>commitlint rejects "bad commit message", accepts "feat(studio): add canvas component", husky commit-msg hook file exists and is executable</done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds with all workspace packages resolved
2. `pnpm -r run typecheck` passes with zero errors
3. `reuse lint` passes (all files SPDX-compliant)
4. All 5 FINOS governance files exist and are linked from README
5. `echo "bad" | pnpm exec commitlint` fails; `echo "feat(ci): test" | pnpm exec commitlint` passes
6. `.husky/commit-msg` exists and is executable
</verification>

<success_criteria>
- pnpm workspace with 5 packages (studio, calm-core, calmscript, mcp-server, extensions) all resolving
- TypeScript strict mode compiling across all packages
- Apache 2.0 SPDX headers on all source files, REUSE dep5 covering config/binary files
- CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, NOTICE, MAINTAINERS.md all present
- README links to all governance files
- commitlint + husky enforcing conventional commits with correct scope enum
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance/01-01-SUMMARY.md`
</output>
