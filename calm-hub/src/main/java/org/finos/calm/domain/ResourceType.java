package org.finos.calm.domain;

/**
 * Enumeration of resource types that can be addressed through the front controller.
 */
public enum ResourceType {
    PATTERN,
    ARCHITECTURE,
    FLOW,
    STANDARD,
    INTERFACE
}
