module.exports = {
    docs: [
        {
            type: 'doc',
            id: 'index',
            label: 'Home',
        },
        {{#if (notEmpty nodes)}}
        {
            type: 'category',
            label: 'Nodes',
            items: [
                {{#each nodes}}
                'nodes/{{unique-id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
        {{#if (notEmpty relationships)}}
        {
            type: 'category',
            label: 'Relationships',
            items: [
                {{#each relationships}}
                'relationships/{{unique-id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
        {{#if (notEmpty flows)}}
        {
            type: 'category',
            label: 'Flows',
            items: [
                {{#each flows}}
                'flows/{{unique-id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
    ],
};

