module.exports = {
    docs: [
        {
            type: 'doc',
            id: 'index',
            label: 'Home',
        },
        {{#if (notEmpty nodes)}}
        {
            type: 'category',
            label: 'Nodes',
            items: [
                {{#each nodes}}
                'nodes/{{id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
       {{#if (notEmpty relationships)}}
        {
            type: 'category',
            label: 'Relationships',
            items: [
                {{#each relationships}}
                'relationships/{{id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
        {{#if (notEmpty flows)}}
        {
            type: 'category',
            label: 'Flows',
            items: [
                {{#each flows}}
                'flows/{{id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        }
        {{/if}}
    ]
};
