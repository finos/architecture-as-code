module.exports = {
    docs: [
        {
            type: 'doc',
            id: 'index',
            label: 'Home',
        },
        {{#if (notEmpty nodes)}}
        {
            type: 'category',
            label: 'Nodes',
            items: [
                {{#each nodes}}
                'nodes/{{this.id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        },
        {{/if}}
        {{#if (notEmpty flows)}}
        {
            type: 'category',
            label: 'Flows',
            items: [
                {{#each flows}}
                'flows/{{this.id}}'{{#unless @last}},{{/unless}}
                {{/each}}
            ],
        }
        {{/if}}
    ]
};
