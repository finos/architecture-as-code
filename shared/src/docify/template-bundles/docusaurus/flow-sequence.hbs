```mermaid
sequenceDiagram
title {{name}}
{{#each transitions}}
    {{#if direction}}
        {{#if (eq direction 'destination-to-source')}}
            {{target}} -->> {{source}}: {{summary}}
        {{else}}
            {{source}} ->> {{target}}: {{summary}}
        {{/if}}
    {{else}}
        {{source}} ->> {{target}}: {{summary}}
    {{/if}}
{{/each}}
```