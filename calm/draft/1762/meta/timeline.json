{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://calm.finos.org/draft/1762/meta/timeline.json",
  "title": "Common Architecture Language Model (CALM) Timelines Vocab",
  "type" : "object",
  "properties": {
    "current-moment": {
      "type": "string",
      "description": "The unique-id of the current architecture moment within the timeline."
    },
    "moments": {
      "type": "array",
      "items": {
        "$ref": "#/defs/moment"
      },
      "description": "A list of significant architecture states or points in time, each represented as a 'moment'."
    },
    "metadata": {
      "$ref": "core.json#/defs/metadata"
    },
    "additionalProperties": false
  },
  "defs": {
    "moment": {
      "type": "object",
      "properties": {
        "unique-id": {
          "type": "string"
        },
        "name": {
          "type": "string",
          "description": "A human-readable name for this architecture moment."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the architecture state at this moment."
        },
        "details": {
          "type": "object",
          "properties": {
            "detail": {
              "type": "string",
              "format": "uri",
              "description": "A link to an architecture or sub-timeline document."
            },
            "detail-type": {
              "type": "string",
              "description": "The type of document the detail-url is pointing to (e.g., 'architecture-document', 'sub-timeline').",
              "enum": [
                "architecture",
                "sub-timeline"
              ]
            }
          },
          "required": [
            "detail",
            "detail-type"
          ],
          "additionalProperties": false
        },
        "valid-from": {
          "type": "string",
          "format": "date",
          "description": "The date when this architecture moment came into effect."
        },
        "adrs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "External links to ADRs (Architecture Decision Records) or similar documents that provide context or decisions related to why the architecture changed."
        },
        "metadata": {
          "$ref": "core.json#/defs/metadata"
        }
      },
      "required": [
        "unique-id",
        "name",
        "detail"
      ]
    }
  }
}
