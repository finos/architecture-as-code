apiVersion: v1
kind: Service
metadata:
  name: trades-mcp-server
  labels:
    app: trades-mcp-server
spec:
  selector:
    app: trades-mcp-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
