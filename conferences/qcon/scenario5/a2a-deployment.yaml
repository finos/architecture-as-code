apiVersion: apps/v1
kind: Deployment
metadata:
  name: trades-a2a-server
  labels:
    app: trades-a2a-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trades-a2a-server
  template:
    metadata:
      labels:
        app: trades-a2a-server
    spec:
      containers:
        - name: trades-a2a-server
          image: jpgough/trades-a2a-server:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: QUARKUS_REST_CLIENT__COM_DEMO_TRADESA2A_TRADESAPICLIENT__URL
              value: "http://trades"
            - name: DENIED_SYMBOLS
              valueFrom:
                configMapKeyRef:
                  name: denied-symbols
                  key: denied-symbols
                  optional: true
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
