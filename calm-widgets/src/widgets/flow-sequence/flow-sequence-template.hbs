```mermaid
sequenceDiagram
{{#each transitions}}
{{#if direction}}
    {{#if (eq direction 'destination-to-source')}}
    {{target}} -->> {{source}}: {{description}}
    {{else}}
    {{source}} ->> {{target}}: {{description}}
    {{/if}}
{{else}}
    {{source}} ->> {{target}}: {{description}}
{{/if}}
{{/each}}
```

