```mermaid
sequenceDiagram
    UI ->> Screen Service: UI sends requests to Screen Service
```


```mermaid
sequenceDiagram
    Position Service ->> Account Service: Position Service sends data to Account Service
```