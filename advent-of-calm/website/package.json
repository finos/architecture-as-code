{
  "name": "advent-of-calm-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run copy:content && astro dev",
    "start": "astro dev",
    "build": "npm run copy:content && astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "copy:content": "mkdir -p src/content/days && cp ../day-*.md src/content/days/ && cp ../home.md src/"
  },
  "dependencies": {
    "astro": "^5.15.9",
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
